# Réunion d'équipe WashBoard — vendredi 28 août 2026

## Signaux du jour

**Commits (26 dernières heures) :** fix RLS critique sur l'entonnoir de réservation
publique (les RDV existants n'étaient jamais vus par `/api/slots/smart` et
`lib/travelFee.ts`, lecture via client anonyme sans droits — corrigé en passant par
le service_role) ; ajout de liens de réservation dédiés par réseau social
(Instagram/TikTok/Facebook/Google) avec étiquette "Mon site web" et métriques de
conversion par source/appareil/timing dans le CRM ; fix visuel d'une vignette "Pic
simultané" tronquée sur mobile.

**Santé du code :** `npx tsc --noEmit` propre (0 erreur). `npm run lint` : 0 erreur,
24 warnings (tous pré-existants et déjà tranchés dans le TODO — pattern
`react-hooks/set-state-in-effect` assumé, variables inutilisées mineures).
`npx vitest run` : 277/277 tests passés (24 fichiers).

**`npm audit --omit=dev` :** 9 vulnérabilités (6 hautes, 3 modérées) — toutes déjà
identifiées et documentées dans le TODO, rien de nouveau depuis hier. Le correctif
(mise à jour Next.js 16.2.6 → 16.3.3) est évalué localement sur une branche non
mergée, en attente d'un go de l'agent cyber + test manuel du golden path.

**TODO.md — Priorité haute (ouvert) :** mise à jour Next.js en attente de
validation ; droits d'écriture GitHub de la routine cloud à vérifier (403 constaté
hier) — voir étape 4 de ce rapport pour savoir si le push d'aujourd'hui est passé ;
legal placeholders et passage Stripe en live prévus pour début septembre ;
vérification à faire (config `travel_fee_mode` de Kookii Clean) pour savoir si le
bug RLS a pu sous/sur-facturer de vrais clients en mode "RDV précédent".

**Angle mort assumé :** aucun accès à la base Supabase de production dans cette
routine — aucun signal sur l'activité réelle des clients (réservations, paiements,
usage des nouveaux liens par réseau social) n'est disponible ici.

## Tour de table

### seo-geo
Rien de neuf ces 26 dernières heures — aucun commit dans le blog, `lib/blog.ts`,
les données structurées ou les métadonnées. Point de vigilance à garder : les
nouveaux liens de réservation par réseau social pourraient introduire des URLs à
paramètres de tracking — à vérifier qu'elles ne créent pas de contenu dupliqué
indexable ni de pages parasites dans le sitemap, dès que le détail de
l'implémentation sera regardé. Pas de signal trafic/conversions disponible (accès
Supabase prod manquant).

### cyber
Le fix RLS bookings/travelFee est le bon réflexe (service_role côté serveur), mais
le point resté ouvert dans le TODO — Kookii Clean a-t-elle été sous/sur-facturée
via "RDV précédent" pendant que la lecture échouait — reproduit exactement le motif
"échec silencieux" traqué cette semaine, cette fois sur de la facturation client
réelle. Accès Supabase prod manquant pour trancher : à vérifier en priorité par
quelqu'un qui a l'accès. Ne mergera pas la branche Next.js 16.3.3 avant d'avoir
validé l'impact du changement majeur, même si l'audit n'a rien de nouveau. Le 403
GitHub de la routine n'est pas dans son périmètre mais mérite clarification avec
Alexandre s'il bloque un déploiement.

### dev
Le fix RLS sur l'entonnoir public est le point important du jour — trou
sécurité/facturation corrigé, tsc/lint/277 tests passent proprement. Rien d'autre
dans son périmètre ne préoccupe (liens par réseau social, métriques d'entonnoir et
fix mobile sont des ajouts sans anomalie côté code). Referait : obtenir
confirmation explicite de cyber sur le fix RLS avant de le classer réglé (touche
autorisation + facturation, pas juste un bug UI), et s'assurer qu'aucun merge
prématuré de la branche Next.js 16.3.3 ne se glisse pendant que le focus est sur
RLS.

### ideas
Les liens de réservation par réseau social ont été livrés sans passer par sa
grille — encore dans le lane du pitch tant que ça reste un outil d'attribution
pour la page de démo commerciale, mais à surveiller si ça glisse vers un vrai
dashboard marketing générique ("tu laves des voitures, on gère le reste"). Plus
urgent à ses yeux : avant de classer le fix RLS "réglé", voudrait une confirmation
explicite de cyber qu'il n'y a pas eu de fenêtre d'exposition résiduelle — pas
juste "ça build et les tests passent". Referait : faire passer toute nouvelle
surface de tracking par sa grille avant merge plutôt qu'après ; pas de données
réelles pour juger si l'investissement sur les liens par réseau était justifié.

### growth
Les liens de réservation par réseau social et les métriques de conversion par
source sont exactement ce qui manquait pour transformer la page de démo en
argument commercial concret — bon signal produit. Mais aucune visibilité sur
l'usage réel (Kooki Clean a-t-il recommencé à réserver, ces liens sont-ils déjà
testés par un vrai laveur) — signal manquant, à demander à Alexandre plutôt que de
supposer. Referait : avant toute idée de nouveau canal d'acquisition, veut ce point
d'usage réel en premier. Rien d'autre à signaler côté marketing/commercial
aujourd'hui.

## Priorités du jour

- **Vérifier la facturation Kookii Clean** : confirmer si le bug RLS (mode "RDV
  précédent") a sous/sur-facturé de vrais clients pendant qu'il était actif —
  point ouvert repris par dev, cyber et ideas comme la priorité du jour.
- **Confirmation explicite de cyber sur le fix RLS** avant de le considérer clos
  (pas seulement "build vert + tests passent") — demandé par dev et ideas.
- **Ne pas merger Next.js 16.3.3** sans le go final de cyber et le test manuel du
  golden path, même si l'audit n'a rien de nouveau.
- **Chiffrer l'usage réel des nouveaux liens par réseau social** dès que possible
  (accès Supabase prod manquant ici) — growth et ideas veulent ce signal avant
  d'aller plus loin sur le tracking/marketing.
