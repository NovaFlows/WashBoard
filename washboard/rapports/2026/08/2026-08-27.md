# Réunion d'équipe WashBoard — jeudi 27 août 2026

> ⚠️ **Ce rapport n'a pas pu être poussé sur `master` automatiquement.** `git push` et l'API GitHub renvoient tous les deux un 403 : "Claude doesn't have GitHub access to NovaFlows/WashBoard for your organization" / "Resource not accessible by integration". L'app GitHub installée pour l'organisation n'a apparemment pas (ou plus) les droits d'écriture sur ce dépôt, alors que la lecture fonctionne. Le rapport existe en local (deux copies committées, non poussées) ; il faudra soit qu'Alexandre le pousse lui-même, soit qu'un admin de l'organisation réinstalle/étende les droits de l'app Claude GitHub (voir https://github.com/apps/claude/installations/select_target ou https://claude.ai/customize/connectors) avant que cette routine puisse pousser seule à l'avenir.

## Signaux du jour

- **Commits (26 dernières heures) :** mise en place des cinq agents d'équipe (seo-geo, cyber, dev, ideas, growth) et de leur communication directe, plus la documentation d'une vulnérabilité Next.js relevée par l'agent cyber. Juste avant : validation des données structurées (Rich Results Test), ajout des données structurées sur la page d'accueil, fix nav mobile ("Connexion" visible), fix des nuages invisibles sur mobile.
- **Santé du code :** `tsc --noEmit` propre (0 erreur) · `npm run lint` 0 erreur / 24 warnings (surtout des règles `react-hooks/set-state-in-effect` sur ThemeProvider/ThemeToggle/LandingPage, plus 2 imports/variables inutilisés) · `vitest run` : 231 tests passent sur 22 fichiers.
- **`npm audit --omit=dev` :** 9 vulnérabilités — 6 hautes, 3 modérées (Next.js, postcss, sharp, nanoid, brace-expansion, tmp, uuid, protobufjs). Correspond exactement à ce que le TODO documente déjà.
- **TODO.md — Priorité haute :** mise à jour Next.js 16.2.6 → 16.3.3+ à faire (couvre la faille la plus grave : divulgation non authentifiée d'endpoints Server Function), avec l'avertissement explicite de ne pas lancer `npm audit fix --force` à l'aveugle car ce Next.js est modifié par rapport au standard (voir AGENTS.md). Les autres items priorité haute (placeholders légaux, passage Stripe en live) sont planifiés pour début septembre, pas encore d'actualité.
- **Signal manquant :** pas d'accès à la base Supabase de production dans cette routine — aucune donnée sur l'activité réelle des clients (réservations, paiements, trafic) n'a pu être vérifiée aujourd'hui.

## Tour de table

### seo-geo
Rien de neuf côté SEO/GEO depuis hier — les commits du jour concernent la coordination inter-agents, pas le contenu ou les balises. Les gains d'hier (JSON-LD homepage validé, nav mobile) restent en place sans régression visible. Aucune donnée de trafic ou de position réelle disponible (pas d'accès Supabase prod). Point de vigilance hors périmètre : la maj Next.js pourrait toucher le rendu SSR et donc l'indexation si elle casse quelque chose — au dev de la traiter, seo-geo restera attentif au HTML servi après coup.

### cyber
Rien de neuf par rapport au TODO déjà rédigé hier : la vulnérabilité Next.js (endpoints Server Function exposés sans auth, corrigée en 16.3.3) et les 8 autres vulnérabilités de la chaîne de dépendances sont confirmées telles quelles par le `npm audit` du jour. Pas d'accès prod donc pas de nouvelle vérification RLS/GRANT. Ce qu'il referait : continuer à bloquer tout `npm audit fix --force` tant que l'ampleur réelle du patch 16.3.3 n'est pas confirmée côté dev, vu le Next.js modifié maison. tsc/lint/tests propres — rien ne bloque objectivement la décision, sauf le go/no-go sur le bump.

### dev
Rien de neuf côté code depuis hier — tsc/lint/tests restent propres, les 24 warnings react-hooks sont connus et non bloquants. Le seul point à suivre est le TODO Next.js : dev n'y touchera pas seul, en attente du go de cyber sur l'évaluation d'impact. Ce qu'il referait : sur le fix honeypot récemment mergé (silencieux par construction — faux 201 sans log), vérifier qu'un événement est bien loggé quand une réservation est droppée par le honeypot, pas seulement qu'elle soit droppée.

### ideas
Rien de neuf côté produit aujourd'hui — pas d'accès à la base de prod, donc aucun signal client réel à commenter. Ce qu'il remarque : le bug honeypot corrigé et le TODO Next.js rappellent que les vrais risques du produit en ce moment sont la défaillance silencieuse et la sécurité, pas un manque de fonctionnalités. Rien à proposer tant que la fenêtre Supabase est fermée ; dès que l'accès revient, priorité aux taux d'abandon sur la page de réservation publique.

### growth
Le sujet le plus grave du lot reste le honeypot cassé qui avalait de vraies réservations en silence (faux 201) — sur une page qui sert aussi de démo commerciale, ça pouvait saborder Kooki Clean sans qu'on le voie. Bonne nouvelle : corrigé, et les 41 réservations réelles du client sont intactes. Pas d'accès Supabase aujourd'hui pour vérifier si un ralentissement de réservations s'expliquerait par ce bug — pas de chiffre inventé. Ce qu'il referait : dès l'accès Supabase rétabli, croiser les dates ; par ailleurs, la suppression du plan Business et le passage du multi-laveurs vers Pro (69€) changent la grille tarifaire utilisée en argumentaire — à revérifier dans `lib/plan.ts` avant le prochain échange commercial.

## Priorités du jour

- **Décision Next.js 16.2.6 → 16.3.3+** : cyber et dev sont alignés sur la nécessité mais attendent une évaluation d'impact avant de merger (Next.js modifié maison, ne pas utiliser `--force` à l'aveugle) — c'est le seul blocage actionnable identifié aujourd'hui.
- **Vérifier le logging du honeypot anti-spam** : le bug corrigé était silencieux par nature ; dev propose de confirmer qu'un événement est bien tracé quand une réservation est droppée, pour éviter de reproduire un point mort.
- **Revoir `lib/plan.ts` avant le prochain contact commercial** : la suppression du plan Business et le passage du multi-laveurs vers Pro (69€) ont changé la grille tarifaire — growth veut s'assurer que l'argumentaire est à jour.
- **Signal Supabase manquant** : aucun agent n'a pu vérifier l'activité réelle des clients aujourd'hui (réservations, abandon, trafic) — growth et ideas ont chacun une question en attente (ralentissement éventuel lié au bug honeypot ; taux d'abandon sur la réservation publique) qui nécessite l'accès à la base de prod pour être tranchée.
