# Réunion d'équipe WashBoard — dimanche 30 août 2026

## Signaux du jour

**Commits (dernières 26h) :** 15 commits, journée dense. L'essentiel : un audit de dette technique complet (méthode SonarQube, 197,7h ≈ 24,7 jours, note A) avec réduction dans la foulée (dédup de 4 fonctions dont `haversineKm`, 20 `console.*` → logger structuré, 168 lignes CSS dupliquées factorisées), une couverture e2e passée de 22 à **123 tests** désormais branchés en CI sur un build de production, et deux bugs réels trouvés en écrivant ces tests : la semaine comptable commençait le dimanche (décalage UTC dans `ComptaDashboard`, corrigé) et `plan.ts` (`graceEnded`/`monthsOwed` — blocage de réservations et facturation) n'avait aucun test, désormais à 100% de couverture (pas de défaut trouvé, mais risque comblé).

**Santé du code :** `tsc --noEmit` propre (0 erreur), `npm run lint` → 0 erreur / 23 warnings (inchangé, motif `set-state-in-effect` déjà assumé), `vitest run` → **371/371 tests verts**.

**`npm audit` :** **0 vulnérabilité**, aussi bien en ne comptant que les dépendances de prod (`--omit=dev`) qu'en comptant tout (`npm ci`).

**TODO.md — priorité haute (points encore ouverts) :**
- ⚠️ Action requise d'Alexandre : créer les 7 secrets GitHub pour que le job e2e de la CI teste réellement (sinon il s'arrête proprement sans rien tester).
- La routine cloud "réunion d'équipe" n'avait toujours pas les droits d'écriture GitHub au dernier constat (2026-08-27, 403 au push) — à revérifier au moment du push de ce rapport.
- Deux points planifiés pour début septembre 2026 : remplir les placeholders légaux (mentions légales/CGV/confidentialité) et passer Stripe en live (les forfaits annuels n'existent pas encore côté Stripe).

**Hors de portée aujourd'hui :** pas d'accès à la base Supabase de production — aucun signal vérifiable sur l'activité réelle des clients (réservations, paiements) dans ce rapport.

## Tour de table

### seo-geo
Rien de neuf : le refactoring des dernières 26h (dédup `haversineKm`, zone géographique) n'a touché ni le blog, ni le sitemap, ni les JSON-LD. Pas d'accès Supabase donc aucune donnée trafic à commenter.
Ce que je referais : rien à changer dans ma méthode. Point de vigilance à garder en tête : les placeholders légaux prévus début septembre touchent des pages actuellement fines côté contenu — à surveiller pour ne pas laisser du contenu vide ou dupliqué indexé entre-temps.

### cyber
Un résidu du motif déjà identifié cette semaine ("lecture Supabase échouée → valeur par défaut permissive → silence") traîne encore dans `src/app/api/unavailabilities/route.ts` (route dashboard, distincte de la version publique déjà corrigée) : `return NextResponse.json(data ?? [])` sans vérifier ni logger `error`. Pas une régression du refactoring — un résidu qui n'a simplement pas été retouché. Rien d'autre : npm audit propre, pas de secret exposé repéré dans les commits du jour.
Ce que je referais : garder le réflexe de grep systématique `?? []` / `catch {}` autour de Supabase après chaque gros refactoring, pas seulement sur les fichiers nouvellement touchés — 2 minutes ce matin, un résidu trouvé.

### dev
Le refactoring a touché du code sensible (dédup `haversineKm`, passage console→logger) sans rien casser : tsc/lint/tests restent verts. Rien d'autre à signaler sur mon périmètre aujourd'hui (`lib/plan.ts`, blog, guide, cron, `apiError`/logger).
Ce que je referais : avant de dédupliquer une fonction comme `haversineKm`, vérifier qu'aucune des deux versions ne portait un correctif silencieux (comme le bug UTC du dimanche) — ça aurait pu se perdre dans la fusion.

### ideas
Le fix du bug comptable (semaine démarrant dimanche) est le point notable du jour — le genre d'erreur silencieuse qui aurait pu faire douter Kooki Clean de son propre CA affiché, bon réflexe de l'avoir tracé plutôt que rustiné. Le reste (123 tests e2e, dette technique) est du travail sain mais hors de mon ressort produit.
Ce que je referais : rien à trancher aujourd'hui — pas de nouvelle fonctionnalité ni de demande client remontée dans les signaux du jour, donc pas d'idée à inventer par principe.

### growth
Refactoring, 371 tests et fix comptable solides côté produit, mais aucune visibilité sur l'usage réel (pas d'accès Supabase prod) pour juger si le funnel de réservation renforcé répond à un vrai problème client.
Ce que je referais : demander à Alexandre un point rapide sur l'activité récente de Kooki Clean avant de préparer tout argumentaire commercial — plus urgent que toute idée d'acquisition. Note : cette suggestion vient d'une impression de l'agent, pas d'une donnée vérifiée (ni lui ni ce rapport n'ont accès à Supabase) — à confirmer ou infirmer par Alexandre directement.

## Priorités du jour

- **7 secrets GitHub à créer par Alexandre** pour que la CI e2e (123 tests) teste réellement quelque chose — sans ça, tout ce travail de couverture tourne à vide en CI.
- **`src/app/api/unavailabilities/route.ts`** porte encore le motif « lecture Supabase silencieuse → défaut permissif » identifié cette semaine sur d'autres routes — à corriger (log + éventuellement 500 au lieu de `[]`), signalé par `cyber`, pas corrigé sans validation.
- Droits d'écriture GitHub de la routine cloud à revérifier au moment du push de ce rapport (bloquant récurrent signalé le 2026-08-27, pas encore confirmé résolu).
- Rien d'urgent côté SEO ; côté growth, vérifier avec Alexandre l'activité réelle de Kooki Clean avant toute nouvelle action commerciale — aucun signal chiffré disponible dans ce rapport pour trancher.
