# Réunion d'équipe WashBoard — samedi 29 août 2026

## Signaux du jour

**Commits (dernières 26h) :** 11 commits. L'essentiel : fin de la montée de version Next.js 16.2.6 → 16.3.3 (mergée sur `master`, GO explicite de `cyber` obtenu hier), et une série de correctifs sur le carrousel d'avis clients de la page de réservation publique (défilement figé, puis comportement mobile/desktop affiné). Le reste, ce sont des mises à jour de `TODO.md` (nettoyage comptes test, vérification PayPal, suivi du bug avis).

**Santé du code :** `tsc --noEmit` propre (0 erreur), `npm run lint` → 0 erreur / 24 warnings (inchangé, essentiellement des `setState` synchrones dans des `useEffect`, motif déjà assumé ailleurs dans le projet), `vitest run` → **277/277 tests verts**.

**`npm audit` :** en ne comptant que les dépendances de prod (`--omit=dev`) → **5 vulnérabilités (3 modérées, 2 hautes)**, toutes dans des dépendances d'outils annexes (`brace-expansion`, `protobufjs`, `tmp`, `uuid` via `exceljs`), aucune liée à Next.js ni exposée aux visiteurs du site. En comptant aussi les dépendances de dev, `npm ci` remonte 7 vulnérabilités (1 faible, 3 modérées, 3 hautes) — 2 vulnérabilités supplémentaires purement dev-only.

**TODO.md — priorité haute (points encore ouverts) :**
- Comptes fantômes possibles dans `auth.users` sans fiche `washers` correspondante (trouvé le 2026-08-28, cause racine pas encore confirmée).
- La routine cloud de réunion d'équipe n'avait pas les droits d'écriture GitHub au 2026-08-27 (push refusé en 403) — à revérifier au moment du push de ce rapport.
- Deux points planifiés pour début septembre 2026 : remplir les placeholders légaux (mentions légales/CGV/confidentialité) et passer Stripe en live (avec le point non résolu des forfaits annuels absents côté Stripe).

**Hors de portée aujourd'hui :** pas d'accès à la base Supabase de production — aucun signal sur l'activité réelle des clients (réservations, paiements) dans ce rapport.

## Tour de table

### seo-geo
Rien à signaler côté SEO/GEO — les commits des dernières 26h touchent le carrousel d'avis clients et la montée Next.js, aucun impact sur balises, sitemap ou contenu blog. Point de vigilance : le carrousel affiche des avis clients sur la page de réservation, à vérifier avec `dev` qu'aucun n'est fictif avant d'envisager un jour de les injecter en `aggregateRating` structuré.
Ce que je referais : rien à corriger aujourd'hui. Actions SEO en attente inchangées (attendre une semaine pour Search Console, publier un nouvel article, faire valider les fourchettes de prix par Alexandre) — pas de données de trafic réelles à rapporter.

### cyber
A vérifié `src/app/api/auth/signup/route.ts` sur le point comptes fantômes : un rollback existe déjà (`auth.admin.deleteUser` si l'insert `washers` échoue), donc l'hypothèse la plus probable reste une fiche `washers` supprimée un jour par un `DELETE` direct sans passer par la suppression de l'utilisateur auth associé. Pas encore quantifié combien d'autres comptes fantômes existent (nécessiterait une requête en lecture seule sur la base partagée prod/dev, pas lancée aujourd'hui).
Ce que je referais : proposer à `dev` de tracer l'échec du `deleteUser` de rollback (actuellement silencieux) et préparer une requête de comptage en lecture seule avant toute décision de nettoyage.

### dev
Confirme le rollback existant dans `signup/route.ts`, mais rien n'y est loggué — ni un échec d'insert `washers`, ni un échec du rollback lui-même : même motif d'échec silencieux que les trois bugs de prod déjà corrigés cette semaine. Note aussi que le TODO liste le même défilement figé sur le sélecteur de jour de réservation (`StepSlot.tsx`), pas encore traité, avec le pattern `ReviewsCarousel.tsx` à réutiliser.
Ce que je referais : ajouter un `logger.error` sur l'échec d'insert et sur l'échec du rollback (avec l'`user_id`, sans donnée sensible) — en passant d'abord par `cyber` puisque c'est un chemin d'authentification.

### ideas
Rien à signaler côté produit — pas de nouvelle fonctionnalité livrée dans les dernières 26h, et pas d'accès aux données Supabase de prod pour juger une idée sur un signal réel aujourd'hui.
Ce que je referais : garder comme bonne pratique le fait que la montée Next.js 16.3.3 soit passée par un GO explicite de `cyber` avant merge — à maintenir pour toute évolution touchant l'auth ou la BDD, même quand ça ressemble à une simple mise à jour technique.

### growth
Note positive : le paiement PayPal de l'engagement annuel est validé de bout en bout par Alexandre, et la base est nettoyée (ne restent que Kooki Clean, Kookii Clean, ysclean, BellAuto89). Les correctifs sur le carrousel d'avis clients sont utiles pour la démo commerciale.
Ce que je referais : rien de neuf côté stratégie aujourd'hui. Rappel pour un prochain point : le paiement reste manuel (PayPal/virement) tant que Stripe n'est pas live, l'argumentaire d'onboarding est donc inchangé pour l'instant.

## Priorités du jour

- **Comptes fantômes `auth.users`** : la cause n'est pas un rollback manquant (il existe) mais des échecs silencieux dans `signup/route.ts` — `dev` propose d'ajouter du logging, en coordination avec `cyber` avant de toucher au flux d'auth.
- **`StepSlot.tsx`** porte le même bug de défilement figé que celui corrigé hier sur les avis clients — à reprendre avec le même correctif (`ReviewsCarousel.tsx` comme modèle).
- Aucune vulnérabilité npm nouvelle ni régression de tests aujourd'hui ; le passage Next.js 16.3.3 reste stable en prod.
- Rien d'urgent côté SEO ou growth — les deux agents confirment l'absence de signal nouveau.
