# Réunion d'équipe WashBoard — mercredi 23 septembre 2026

## Signaux du jour

- **Commits (dernières 26h) : 10.** Dominés par la mise en place de l'agent `refonte` (mémoire des décisions de design, skills de design d'Emil Kowalski figées en versions, plan de vol pour lancer la refonte 2026 en un seul prompt, lecture des maquettes). Deux fixes produit : suppression d'un avertissement en doublon sur Disponibilités (gardé côté Prestations), et précision du message du plafond de durée + du jour posant problème sur Prestations. Plus le rapport de la réunion du 22/09.
- **Code :** `tsc --noEmit` OK (0 erreur) · lint OK (0 erreur, 30 warnings — surtout des `setState` synchrones dans des `useEffect` sur `LandingPage.tsx`, `ThemeProvider.tsx`, `ThemeToggle.tsx`, plus 2 variables inutilisées) · **1001 tests passent** (84 fichiers) · `npm audit --omit=dev` : **0 vulnérabilité en production** (3 modérées, mais uniquement en devDependencies).
- **TODO.md — priorité haute :**
  1. **Quota Supabase.** Restriction des projets possible dès le **5 octobre 2026** (dépassement de quota organisation signalé le 14/09). Base et stockage très loin des limites (14 Mo / 2,1 Mo) : la cause probable est l'**egress**, les fonds/logos étant servis directement depuis Supabase sans passer par le cache Vercel, amplifiés par une vidéo TikTok virale du 04/09.
  2. **Audit post-lancement Ryan (21/09, liste "20 points").** 15/20 déjà en place. 5 restants : vitesse de la page de réservation (LCP 3,1 s dont 2,3 s de TTFB), contraste des couleurs (WCAG AA, plusieurs échecs dont `text-slate-400`), image de prévisualisation manquante sur l'accueil (og:image/twitter:image), CGU absentes, case à cocher CGV/confidentialité absente à l'inscription.
- ⚠️ **Signal manquant :** aucun accès à la base Supabase de production dans cette routine → rien à dire sur l'activité réelle des clients (réservations, paiements, usage Kooki Clean). Idem pour Vercel Analytics : aucun token configuré, aucune donnée de trafic vérifiable.

## Tour de table

### seo-geo
L'og:image/twitter:image manquante sur l'accueil le concerne directement (partage social, et les moteurs génératifs s'appuient aussi sur ces métadonnées). Le reste de l'audit (LCP, contraste, CGU, CGV) n'est pas son terrain. Rien de neuf côté blog ou sitemap. Il referait : traiter l'og:image en même temps que le fix LCP, souvent la même image statique à optimiser.

### cyber
Le chantier CGU/CGV est le bon réflexe : sans case à cocher horodatée, l'acceptation n'est pas opposable — veut relire `/signup` avant merge pour vérifier que le consentement est tracé en base, pas juste côté client, et que le texte passe d'abord par `legal`. Les 3 vulnérabilités modérées en devDependencies n'affectent pas le bundle prod livré : pas de chantier isolé, à regarder au fil de l'eau. Rien d'autre à signaler côté sécurité sur les fixes du jour.

### dev
Le quota Supabase est le vrai sujet : `getBgStyle` sert les fonds/logos en URL publique directe sans `/_next/image`, ce qui explique l'egress qui grimpe, et avec le pic TikTok il ne reste probablement pas deux semaines de marge avant le 5/10. Probablement aussi la cause du TTFB de 2,3 s sur la page de réservation — même défaut d'assets non cachés sur la page la plus vue des clients. Il referait : migrer `getBgStyle` vers `/_next/image` en premier geste, confirmer la cause côté Supabase Usage avant de décider Pro ou non, puis remesurer LCP/TTFB à froid (une seule mesure ne suffit pas). La case CGU/CGV attend `legal`, pas bloquante.

### ideas
Rien de nouveau qui appelle une idée produit aujourd'hui — les commits sont de l'outillage interne et des fixes UX mineurs, pas des signaux clients. Le quota Supabase est un sujet dev/infra urgent, pas un sujet idées. Sans données clients réelles, toute proposition serait de l'intuition, pas un fait.

### growth
Le risque de coupure Supabase le 5 octobre est le vrai sujet growth : si ça coupe les pages de réservation en pleine traction post-TikTok, ça touche l'outil de démo commerciale au pire moment. L'og:image manquante sur l'accueil casse aussi le partage social, alors qu'une vidéo virale génère justement des partages — coût de correction faible vu l'impact. Rien de neuf côté commercial sinon ; toujours aucune visibilité sur l'usage réel de Kooki Clean.

### legal
Les deux chantiers de l'audit Ryan sont fondés — l'absence de trace d'acceptation CGV/confidentialité à `/signup` est le vrai point de risque (opposabilité en cas de litige), les CGU sont secondaires en comparaison. Le point mineur sur `/confidentialite` (mesure d'audience Vercel Analytics non mentionnée) est facile à corriger, à grouper avec la même passe de rédaction. Prépare un brouillon de case à cocher + mention à valider par Alexandre avant transmission à `dev`, coordination avec `cyber` pour le stockage de la preuve d'acceptation.

### designer
Deux problèmes concrets sur le site public. Le contraste (`text-slate-400` en gris clair sur fond blanc) est illisible pour une partie des visiteurs — pied de page, dates d'articles, mentions légales, onglet actif de réservation : un vrai frein de confiance, pas un détail cosmétique. L'og:image manquante sur l'accueil peut afficher une vignette vide quand Alexandre partage le lien en démo. Referait : durcir les tons de gris (slate-500/600) et créer `opengraph-image.tsx` sur le modèle du blog, preuve par capture avant/après. Le CLS n'est pas son sujet mais y jettera un œil après.

### prospection
Rien à signaler aujourd'hui — aucun numéro ni demande d'analyse reçu d'Alexandre. Note l'alerte quota Supabase : si les pages de réservation sautaient, ça toucherait directement l'argument de démo pendant les futurs appels de prospection.

### video
Rien à signaler aujourd'hui, aucune tâche en cours. Point positif : le TikTok du 04/09 continue de faire parler de lui, probablement à l'origine du pic d'egress Supabase — signe que la vidéo a bien généré du trafic, mais le sujet est côté dev/infra.

### sentry
Rien à signaler aujourd'hui — aucune alerte, aucun errorId ni digest remonté. Pas d'accès direct au dashboard Sentry dans cette routine (repose sur ce qui est rapporté) ; à vérifier que `NEXT_PUBLIC_SENTRY_DSN` est bien configurée côté Vercel avant de conclure au calme plat.

### analytics
Aucun accès configuré aujourd'hui : pas de `VERCEL_TOKEN` pour l'API Vercel Analytics, pas d'accès à la base Supabase de prod. Rien à confirmer ni infirmer sur le trafic ou les réservations réelles. Seul indice indirect : la vidéo TikTok virale du 04/09 mentionnée dans le TODO comme cause probable d'un pic de trafic — un indice, pas une mesure.

## Priorités du jour

- **Quota Supabase avant le 5 octobre** : migrer les fonds/logos vers `/_next/image` (`getBgStyle`) pour réduire l'egress, confirmer la cause exacte côté Supabase Usage, décider Pro si besoin. Convergence dev + le chantier vitesse de la page de réservation (LCP/TTFB) de l'audit Ryan.
- **CGU + case CGV/confidentialité à l'inscription** : `legal` prépare les textes et la mécanique de trace d'acceptation, `dev` implémente, `cyber` relit avant merge (le flux d'inscription est touché).
- **Contraste des couleurs + og:image manquante sur l'accueil** : deux correctifs `designer` à faible coût, impact direct sur la lisibilité publique et le partage (démo commerciale, réseaux sociaux).
- **Signaux manquants à combler** : aucun accès à Supabase prod (réservations/paiements réels) ni à Vercel Analytics (token absent) dans cette routine — à arbitrer si Alexandre veut que `analytics` et le reste de l'équipe travaillent sur des données réelles plutôt que des indices indirects.
