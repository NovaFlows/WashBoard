# Réunion d'équipe WashBoard — mardi 1 septembre 2026

## Signaux du jour

- **Commits (26h) :** `feat(signup)` téléphone obligatoire et unique à l'inscription, `fix(parametres)` normalisation du téléphone modifié depuis les réglages, `docs(todo)` remontée du signal produit BellAuto89.
- **Santé du code :** `tsc --noEmit` OK, lint OK (23 warnings connus, motif `set-state-in-effect` assumé dans `eslint.config.mjs`), **386/386 tests unitaires verts**, `npm audit --omit=dev` → **0 vulnérabilité**.
- **TODO.md — priorité haute :** signal produit BellAuto89 (voir tour de table), action requise d'Alexandre pour créer 7 secrets GitHub afin que le job e2e de la CI tourne réellement, et deux échéances planifiées début septembre 2026 (placeholders légaux, passage Stripe en live avec les forfaits annuels à créer côté Stripe).
- **Angle mort assumé :** pas d'accès à la base Supabase de production dans cette routine — aucune donnée sur l'activité réelle des clients (réservations, paiements) n'est disponible aujourd'hui.

## Tour de table

### seo-geo
Rien à signaler aujourd'hui : les commits et le TODO touchent l'onboarding, un bug UX potentiel et de l'infra CI, aucun impact sur contenu, balises ou sitemap. Je referais pareil : pas de check technique sans signal, faute de données trafic/conversion. Point de vigilance à garder pour plus tard : si le blocage BellAuto89 touche la page de réservation publique elle-même, il faudra vérifier l'absence d'impact indexation/GEO — mais rien à faire aujourd'hui.

### cyber
Rien d'alarmant. J'ai vérifié le commit signup+téléphone : bon réflexe des devs — échec de lecture renvoie 503 plutôt qu'un passage silencieux, normalisation cohérente entre signup et `PATCH /api/washer` pour que l'unicité ne soit pas contournable par un format différent du même numéro. Point d'attention réel mais pas nouveau : la contrainte UNIQUE SQL sur le téléphone reste à appliquer côté Supabase (donnée à Alexandre en chat) — tant qu'elle n'y est pas, la protection applicative seule est contournable. À reconfirmer que c'est posé avant de considérer le sujet clos.

### dev
Bon signal de fiabilité aujourd'hui (téléphone unique, normalisation cohérente, 386/386, 0 vuln), et le blocage BellAuto89 est correctement escaladé en TODO (designer + ideas) plutôt que patché à la va-vite. Rien touché côté code aujourd'hui : j'attends l'arbitrage designer/ideas avant de toucher à l'écran Prestations. Les 7 secrets GitHub CI et le passage Stripe live/annuel restent des actions côté Alexandre.

### ideas
J'ai vérifié le code (`PrestationsManager.tsx`, `api/services/route.ts`) : l'hypothèse « bouton désactivé tant qu'on n'a pas choisi catégorie + type » ne tient pas — `canSave` ne teste que nom/prix/durée, catégorie et types sont facultatifs. BellAuto89 penche donc plutôt vers « interrompu » qu'un vrai blocage de saisie. En creusant, j'ai trouvé un **vrai bug adjacent, silencieux** : une prestation enregistrée sans aucun type de véhicule (autorisé sans avertissement par le formulaire) empêche ensuite la page de réservation publique d'afficher le moindre compteur — le bouton « Continuer » reste désactivé sans message (`StepService.tsx`). Je passe le jugement de perception de l'écran à `designer` comme prévu, et je signale ce trou concret à `dev` (correctif de l'ordre de quelques heures).

### growth
BellAuto89 a fait tout le travail fastidieux d'onboarding (logo, adresse, 28 créneaux, 2 catégories) mais zéro prestation créée : sa page publique est vide, il n'a jamais vu le produit fonctionner, et le rendez-vous avec Alexandre a été manqué — pire moment pour décrocher, essai expirant le 2026-09-19. Je referais l'appel de relance en ouvrant sur « ta page ne peut encore rien proposer, il te manque juste les prestations » plutôt que sur le rendez-vous manqué, et je proposerais de créer 2-3 prestations ensemble en 5 minutes au téléphone plutôt qu'un nouveau rendez-vous formel qu'il pourrait re-manquer. Si une relance SMS/email est aussi prévue en plus de l'appel, ça vaut la peine de vérifier avec `legal` que c'est couvert par le consentement d'inscription.

## Priorités du jour

- **BellAuto89 :** un vrai bug a été trouvé (prestation sans type de véhicule → page de réservation publique bloquée silencieusement, sans message) à corriger par `dev` ; en parallèle `designer` doit juger la perception de l'écran Prestations ; `growth` recommande un appel axé sur « il ne manque que les prestations » avant l'expiration de l'essai le 2026-09-19.
- **Sécurité :** confirmer que la contrainte UNIQUE SQL sur le téléphone a bien été posée côté Supabase — la protection applicative seule reste contournable en attendant.
- **Action Alexandre toujours en attente :** créer les 7 secrets GitHub pour que le job e2e de la CI teste réellement quelque chose.
- **Échéances de début septembre 2026 à ne pas perdre de vue :** placeholders légaux (mentions légales, CGV, confidentialité) et passage de Stripe en live (forfaits annuels à créer côté Stripe).
