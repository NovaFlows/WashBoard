# Réunion d'équipe WashBoard — samedi 26 septembre 2026

## Signaux du jour

- **Commits (26h, 6 au total, `washboard/`) :** `perf(réservation)` qui arrête de charger les rendez-vous pour qui ne réserve pas ; un `fix(suivi)` sur le datage des mises à jour ("dater ce que le LAVEUR modifie, pas ce que le cron écrit") poussé (`70dda15`), annulé par un revert (`56267c8`), puis réappliqué (`59ba69b`) — tout dans la même fenêtre de 26h ; un `chore(agents)` qui reprend le bloc `AGENTS.md` régénéré par `next dev` ; le rapport de la réunion d'hier.
- **Code :** `tsc --noEmit` propre (0 erreur) · ESLint 0 erreur / 29 warnings (23 mentionnés il y a quelques jours dans le TODO — légère hausse, mais rien de neuf lié aux commits du jour d'après `dev`/`sentry`) · `vitest run` **1009/1009 tests verts** (85 fichiers).
- **`npm audit --omit=dev`** (dépendances de production) : **0 vulnérabilité**. (`npm ci` sur l'ensemble du projet, dev inclus, signale 3 vulnérabilités modérées, hors périmètre production.)
- **TODO.md — Priorité haute :**
  1. **URGENT avant le 5 octobre 2026** — quota Supabase (bande passante egress) dépassé ; risque de coupure des pages de réservation publiques de tous les laveurs si rien n'est fait d'ici là. Cause probable : fonds/logos servis directement depuis Supabase storage, sans passer par l'optimisation d'image Next (`/_next/image`) ni le cache Vercel.
  2. **Audit post-lancement du 21/09** (Ryan) : 15/20 points déjà en place, 5 restants — vitesse de la page de réservation (LCP 3,1 s / TTFB 2,3 s), contraste des couleurs (WCAG AA), image de partage manquante sur l'accueil, CGU manquantes, acceptation CGV non tracée à l'inscription. Question ouverte pour Alexandre dans le TODO : qui prend les points qui touchent son périmètre (accueil, réservation, inscription) ?
- ⚠️ **Pas d'accès à la base Supabase de production dans cette routine** : aucun signal sur l'activité réelle des clients (réservations, paiements).

## Tour de table

### seo-geo
Confirmé en dur : l'accueil (`layout.tsx`) n'a ni og:image ni twitter:image, contrairement au blog et aux pages de réservation qui en ont déjà — mauvais pour les aperçus de lien sur les réseaux, sujet visuel donc pour `designer`. Si le quota Supabase saute, c'est aussi un risque SEO direct (des pages en erreur recrawlées peuvent être déclassées) — argument de plus pour prioriser ce ticket avant le 5 octobre. Détail mineur : `robots.txt` bloque `/register`, une route qui n'existe pas (la vraie est `/signup`), sans gravité.

### cyber
Rien à signaler côté sécurité applicative aujourd'hui : audit prod à 0 vulnérabilité, aucune nouvelle route ni table touchée par les commits. Le cycle fix→revert→reapply sur le suivi des rendez-vous a été vérifié : cause précise (colonne ajoutée en base, absente en prod au moment du premier déploiement), corrigé proprement avant réapplication — pas un signal d'alerte, mais un rappel utile de la règle « schéma avant code » sur une base partagée dev/prod. En attente pour la relecture du chantier CGU/CGV : pas de diff à examiner tant que rien n'est implémenté.

### dev
Le va-et-vient sur le fix « suivi » interroge un peu mais semble maîtrisé ; à vérifier qu'un test couvre bien le cas « cron vs laveur » pour que ça ne reparte pas en boucle. 29 warnings ESLint (contre 23 mentionnés précédemment) à surveiller sans que ce soit bloquant. Priorité affichée : le quota Supabase egress d'abord (deadline dure au 5/10), puis la vitesse de la page de réservation, puis la case CGU/CGV seulement une fois le texte validé par `legal` — pas d'interprétation perso sur du juridique. tsc/lint/tests/audit tous verts.

### ideas
Pas de nouvelle idée aujourd'hui : le seul sujet qui compte est le quota Supabase egress avant le 5 octobre — un risque de coupure du produit, pas une piste à explorer. Le commit `perf(réservation)` va dans le bon sens mais rien ne dit qu'il suffit à repasser sous le quota : à vérifier plutôt qu'à supposer réglé. CGU/CGV : décision légale avant tout, pas un simple ticket produit — verdict du jour, tuer toute nouvelle proposition tant que l'egress n'est pas confirmé résolu.

### growth
Rien de neuf côté argumentaire ou campagne. Priorité affichée : le quota Supabase egress, qui menace directement l'outil de démo commerciale de Kooki Clean (la bêta actuelle).
⚠️ **Note du manager :** `growth` a de nouveau évoqué « un client réel qui ne réserve plus depuis plusieurs semaines ». Ce signal a déjà été signalé comme **non vérifié** dans les rapports des 20, 22 et 25 septembre — cette session n'a toujours pas d'accès à la base de production pour le confirmer. Il est repris aujourd'hui sans nouvelle donnée : à ne pas prendre pour un fait établi, seul Alexandre peut trancher via le vrai tableau de bord.

### legal
Les points CGU (manquantes) et acceptation CGV (non tracée à l'inscription) sont fondés : sans case à cocher et horodatage, ces documents sont difficilement opposables en cas de litige (impayé, résiliation contestée). Propose de regrouper les deux dans un seul chantier (une case CGU + CGV + confidentialité, un seul horodatage stocké) plutôt que deux ajouts séparés — plus simple à faire relire par `cyber`. Aucune rédaction faite aujourd'hui (point de statut, pas session d'écriture) : prêt à livrer le brouillon dès qu'Alexandre confirme qui prend la main sur ce périmètre.

### designer
D'accord avec les deux points de l'audit qui le concernent. Le contraste (`text-slate-400` et l'onglet actif de la page de réservation à 2,56:1) est un vrai problème de lisibilité, pas juste un chiffre d'audit — corrigeable en passant les gris/bleus trop clairs à des teintes plus foncées de la même famille, sans toucher à la palette de base. L'image de partage manquante sur l'accueil donne une mauvaise première impression sur les liens partagés (SMS/WhatsApp) : à refaire sur le modèle de `blog/opengraph-image.tsx`. Aucun code touché aujourd'hui, prêt à fournir un avant/après dès le feu vert.

### prospection
Rien à signaler aujourd'hui — aucun numéro ni consigne reçue d'Alexandre récemment.

### video
Rien à signaler aujourd'hui — aucun montage en cours ni vidéo confiée. Aucune voix-off ni rendu lancé pendant la réunion.

### sentry
Rien à signaler — aucune alerte, `errorId` ou digest transmis, et pas d'accès réel au dashboard Sentry depuis cette routine (seules les clés placeholder de `.env.example`). Les commits du jour touchent une zone sensible (statut des rendez-vous) mais rien ne remonte en erreur pour l'instant.

### analytics
Aucun accès disponible aujourd'hui : ni jeton Vercel Analytics configuré dans cette routine, ni accès à la base Supabase de production — impossible de dire quoi que ce soit sur le trafic ou le funnel réel d'un laveur. Confirme, sans pouvoir le quantifier, que le point og:image manquant sur l'accueil reste ouvert depuis au moins le 25/09.

## Priorités du jour

- **Quota Supabase (egress) avant le 5 octobre** : risque de coupure des pages de réservation publiques de tous les laveurs — priorité unanime (dev, ideas, growth, seo-geo). À vérifier si le commit `perf(réservation)` du jour suffit, ou s'il faut encore migrer les fonds/logos vers `/_next/image`.
- **CGU + acceptation CGV à l'inscription** : fondé juridiquement, prêt côté dev une fois le texte validé par legal — attend la décision d'Alexandre sur qui prend la main (lui-même ou l'équipe, question posée dans le TODO).
- **Contraste des couleurs + image de partage de l'accueil** : les deux derniers points de l'audit Ryan sont confirmés par designer/seo-geo/analytics, corrections ciblées prêtes à être faites dès le feu vert.
- ⚠️ **Signal non vérifié à trancher par Alexandre** : `growth` répète pour la 4ᵉ fois (20, 22, 25 puis aujourd'hui 26/09) qu'un client aurait arrêté de réserver, sans nouvelle donnée à l'appui — seul un accès au vrai tableau de bord peut confirmer ou infirmer ce point.
