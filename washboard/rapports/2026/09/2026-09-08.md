# Réunion d'équipe WashBoard — mardi 8 septembre 2026

## Signaux du jour

- **Commits (26 dernières heures) :** 7 commits, tous côté prospection/commercial ou ménage —
  retrait de ysclean et « kookiclean » du suivi client, régénération de la feuille Clients,
  durcissement du `.gitignore` du dossier de prospection, ajout d'une colonne Prénom (liste
  fermée Ryan/Yanis/Alexandre) —, plus un fix produit (« afficher aussi l'annonce sur
  ordinateur ») et la clôture du point TODO sur les droits GitHub de la routine quotidienne.
  Rien côté SEO, sécurité ou fonctionnalités majeures cette nuit.
- **TODO.md § Priorité haute (2 points ouverts) :**
  1. Signal produit **BellAuto89** : inscrit ayant tout configuré (logo, adresse, 28 créneaux,
     2 catégories) sauf ses prestations → 0 réservation possible sur sa page publique, et un
     rendez-vous manqué avec Alexandre. À départager entre simple interruption et vrai défaut
     d'écran — demande explicite d'un jugement `designer`/`ideas`.
  2. **Action requise d'Alexandre** : créer les 7 secrets GitHub (Supabase + comptes de test)
     pour que le job e2e de la CI tourne réellement — aujourd'hui il s'arrête proprement avec
     un avertissement, sans rien tester.
- **Santé du code :** `tsc --noEmit` propre, lint **0 erreur / 23 warnings** (motif
  `react-hooks/set-state-in-effect` assumé, documenté), **616/616 tests unitaires verts**
  (`vitest run`, 45 fichiers), `npm audit --omit=dev` **0 vulnérabilité** (`npm ci` aussi :
  0 vulnérabilité toutes dépendances confondues).
- **Angle mort assumé :** pas d'accès à la base Supabase de production depuis cette session —
  aucune donnée sur l'activité réelle des clients (réservations, paiements, trafic) aujourd'hui.
  Ce signal manque, il n'est pas simulé.

## Tour de table

### seo-geo
Rien à signaler aujourd'hui — les commits de la nuit concernent le suivi prospects/clients et
un fix d'affichage, aucun impact sur le blog, les balises ou les données structurées. Je
resterai attentif si le point BellAuto89 débouche sur une évolution de la page de réservation
(ça toucherait potentiellement le contenu de cette page), mais c'est à `designer`/`ideas` de
trancher d'abord.

### cyber
Rien de nouveau côté sécurité aujourd'hui — les commits sont du suivi commercial et un fix
d'affichage, pas de code touchant auth/RLS/paiement. Les deux points de priorité haute ne sont
pas des sujets sécurité pour moi. Point de vigilance sans urgence : le `npm audit --omit=dev`
à 0 vulnérabilité est cohérent avec la trajectoire connue (9 → 5 → 0 vulnérabilités entre le
27 et le 30 août via la mise à jour Next.js), mais je n'ai pas pu le revérifier contre l'état
réel de prod depuis cette session. Ce que je referais : dès accès prod, revérifier
concrètement les GRANT/RLS plutôt que de me fier aux seuls tests unitaires, qui ne couvrent pas
ce type d'échec silencieux.

### dev
Le cas BellAuto89 est le vrai signal du jour. `ideas` a creusé le code (`PrestationsManager.tsx`)
et trouvé la cause précise : le bouton Enregistrer (`canSave`) ne vérifie jamais que
`vehicle_types` contient au moins un type — une prestation peut s'enregistrer sans aucun type
coché (catégorie « Sans catégorie », ou décocher manuellement tout après le pré-cochage du
4 septembre), et rend alors 0 réservation possible, sans aucun message. Le trial de BellAuto89
chevauche ce correctif du 4 septembre : impossible de dire s'il était protégé. Ce que je
referais : ajouter la validation manquante (`vehicle_types.length > 0`) avec un message inline
explicite dans `canSave` — quelques heures de travail, pas une refonte — avant de rejouer un
appel avec BellAuto89. Rien d'autre à signaler aujourd'hui (secrets GitHub CI, ménage
prospection : hors de mon terrain).

### ideas
Verdict : **construire**, pas un simple debrief. J'ai lu `PrestationsManager.tsx` — le bouton
Enregistrer (`canSave`, ligne 83) ne vérifie que nom/prix/durée, jamais `vehicle_types.length`.
Le pré-cochage ajouté le 4 septembre (commit `380796a`) réduit le risque mais laisse deux trous
vivants : choisir « Sans catégorie » vide `vehicle_types` sans avertir, et décocher tous les
types manuellement laisse le bouton actif. Le trial de BellAuto89 (20 août–19 sept) chevauche
ce fix : on ne peut pas conclure qu'il en a bénéficié — et même s'il en a bénéficié, le trou
« sans catégorie »/désélection totale reste ouvert pour tout nouvel inscrit aujourd'hui. Ce que
je referais : passer ce point à `dev` en priorité (pas à `designer` comme le suggérait le TODO)
— c'est un garde-fou de formulaire manquant, pas un problème visuel ; `designer` n'a rien à
juger tant que la validation n'existe pas.

### growth
Le signal du jour, c'est BellAuto89 — essai en cours jusqu'au 19/09, jamais aucun créneau
visible sur sa page publique, et un rendez-vous manqué avec Alexandre en plus : exactement le
genre de friction d'onboarding qui coûte une conversion, avant même de parler d'argumentaire
commercial. Je n'ai pas de quoi trancher si c'est un oubli ou un vrai trou d'UX — ça revient à
`dev`/`ideas`. Ce que je referais : rien à changer côté discours aujourd'hui, mais ce cas
confirme que la page de démo doit être infaillible avant tout effort d'acquisition. Pas de
données de conversion accessibles pour aller plus loin aujourd'hui.

## Priorités du jour

- **BellAuto89 / validation manquante sur l'écran Prestations :** `ideas` a localisé précisément
  le bug (`canSave` dans `PrestationsManager.tsx` n'exige jamais `vehicle_types.length > 0`) et
  rend un verdict clair — à construire, à passer à `dev` en priorité (pas seulement `designer`
  comme prévu initialement). Risque : touche potentiellement tout nouvel inscrit, pas seulement
  BellAuto89.
- **Action toujours en attente d'Alexandre :** créer les 7 secrets GitHub pour activer le job
  e2e de la CI.
- **Rien d'anormal côté code/sécurité/SEO** aujourd'hui — tsc, lint, 616 tests et npm audit tous
  au vert ; `cyber` signale juste vouloir revérifier les GRANT/RLS en prod dès qu'il y aura accès.
