# Réunion d'équipe WashBoard — mercredi 2 septembre 2026

## Signaux du jour

**Commits (dernières 26h) :** 5 — `feat(health)` le contrôle de santé détecte les GRANT
manquants, `feat(push)` notifications de nouvelle réservation sur le téléphone,
`feat(pwa)` WashBoard installable sur l'écran d'accueil, `fix(mobile)` trois défauts
d'affichage sur téléphone, `chore(rapport)` réunion d'équipe du 2026-09-01.

**Santé du code :** `tsc --noEmit` 0 erreur · lint 0 erreur (23 avertissements connus,
motif `react-hooks/set-state-in-effect` assumé) · `vitest run` **396/396 tests verts**
· `npm audit --omit=dev` **0 vulnérabilité**.

**TODO — Priorité haute :** signal produit sur **BellAuto89** (inscrit le 2026-08-20,
essai jusqu'au 2026-09-19) : tout configuré (logo, adresse, 28 créneaux, 2 catégories)
sauf ses prestations → 0 réservation possible sur sa page publique, jamais vu le produit
fonctionner ; RDV convenu avec Alexandre, non honoré. Autres points ouverts : les 7
secrets GitHub à créer par Alexandre pour que le job e2e de la CI tourne réellement (sans
eux le job s'arrête proprement mais ne teste rien) ; le point "droits d'écriture GitHub de
la routine" — **vérifié aujourd'hui : résolu**, les rapports du 2026-08-27 au 2026-09-01
sont bien présents dans l'historique de `master`.

**Absent aujourd'hui :** aucun accès à la base Supabase de production — impossible de dire
quoi que ce soit sur l'activité réelle des clients (nouvelles réservations, paiements).

## Tour de table

### seo-geo
Les 5 commits touchent PWA, push, santé, dashboard mobile — tout côté produit/dashboard
authentifié, rien sur le blog, le sitemap, les JSON-LD ou les balises publiques. Le
manifest PWA démarre sur `/dashboard` (zone non indexée), donc aucun impact SEO/GEO à
vérifier de mon côté aujourd'hui. Rien à signaler sur mon terrain ; le sujet BellAuto89
(écran Prestations) reste hors périmètre, à laisser à `designer`/`ideas`.

### cyber
Le health-check `/api/health` couvre bien le motif que je surveille : il liste
explicitement les tables et nomme celle en défaut au lieu d'un `dbOk: false` générique,
et le commit est honnête sur sa limite (le test e2e passe par `authenticated`, l'incident
réel était côté `service_role` — c'est le health check qui couvre ce chemin, pas le test).
Bonne discipline post-incident. Rien à corriger ; point de vigilance pour la suite :
`TABLES_SERVEUR` est une liste statique à maintenir à la main — toute nouvelle table
touchée par une route serveur doit y être ajoutée à sa création, sinon on revient au point
de départ (silence).

### dev
Les trois derniers commits (PWA, push, health-check GRANT) touchent au front mobile et à
des scopes techniques nouveaux (Service Worker, notifications) sans avoir vérifié
moi-même que les nouvelles routes push/health sont couvertes par des tests dédiés (396/396
passent et tsc/lint sont clean globalement, mais ça ne garantit pas que ce code précis a
sa propre couverture). Je vérifierais que les nouvelles fonctions ajoutées dans `src/lib`
ont bien des tests unitaires dédiés, vu les seuils de couverture stricts de la CI — une
fonction pure sans test y ferait échouer le build silencieusement au prochain push.
BellAuto89 reste le vrai irritant du jour, mais c'est business, pas code.

### ideas
J'ai lu `PrestationsManager.tsx` : `canSave = name && price && duration_minutes`, sans
condition sur catégorie ni sur types de véhicule. L'hypothèse du TODO ("bouton désactivé
tant qu'on n'a pas coché un type") ne colle donc pas au code actuel — on peut
techniquement enregistrer une prestation sans aucun type sélectionné. Soit le blocage est
ailleurs (validation serveur, confusion visuelle entre "Catégorie" et "Types"), soit
BellAuto89 a simplement été interrompu : le signal ne prouve pas un bug UX. Ce que je
referais : demander à `dev` de vérifier le chemin serveur (rejet silencieux sans
`vehicle_types`, ou prestation orpheline invisible côté page publique) avant de faire
juger l'écran par `designer` — verdict "à valider" plutôt que "chantier UX" tant que ce
n'est pas confirmé.

### growth
BellAuto89 est configuré à 100 % sauf les prestations — c'est un pas franchi et pas fait,
à friction quasi nulle. Couplé au no-show au RDV, c'est un signal d'hésitation ou de
manque de temps, pas de désintérêt. Ce que je referais : une relance courte et concrète,
pas commerciale — "il te reste une étape, 5 minutes, je le fais avec toi si tu veux"
plutôt que reproposer un rendez-vous formel déjà raté une fois. Priorité d'appel haute vu
l'essai qui expire le 2026-09-19. Sans accès aux données de production, rien à ajouter sur
les autres clients aujourd'hui.

## Priorités du jour

- **BellAuto89 : vérifier la cause réelle avant d'agir sur deux fronts.** `ideas` a
  trouvé que le formulaire Prestations n'impose pas techniquement de type de véhicule
  (contrairement à l'hypothèse du TODO) — `dev` doit d'abord confirmer si le blocage est
  côté serveur ou juste un abandon en cours de route, avant de lancer `designer` sur un
  chantier UX qui ne serait pas nécessaire. En parallèle, `growth` recommande une relance
  courte et pratique ("je le fais avec toi") plutôt qu'un nouveau RDV formel, à mener
  rapidement vu l'échéance d'essai du 2026-09-19.
- **Couverture de tests des nouveautés PWA/push/health à vérifier.** `dev` signale ne
  pas avoir confirmé que les fonctions ajoutées dans `src/lib` pour ces trois commits ont
  leurs propres tests unitaires — à checker avant que ça ne bloque un futur build sur les
  seuils de couverture CI.
- **7 secrets GitHub toujours en attente côté Alexandre** pour que le job e2e (123 tests)
  tourne réellement en CI — rappel qui reste valable.
- Aucune priorité de sécurité nouvelle : `cyber` valide le health-check GRANT du jour,
  juste un point de vigilance sur la maintenance de sa liste de tables.
