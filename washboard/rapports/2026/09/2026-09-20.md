# Réunion d'équipe WashBoard — dimanche 20 septembre 2026

## Signaux du jour

- **Commits (26h) :** 11 commits, tous produit. Trois sur le blog (`feat(blog): ouvrir le blog à tous les métiers du nettoyage à domicile`, `feat(blog): quatre nouveaux articles sur les recherches non couvertes`, `feat(blog): renforcer le référencement des articles existants`), quatre sur la réservation (`sur téléphone, amener au choix du véhicule`, `revenir à une étape en cliquant sur la barre d'étapes`, `un vrai calendrier mensuel pour choisir sa date`, `choisir les suppléments voiture par voiture`), un sur les notifications (`toucher une notification ouvre la fiche du rendez-vous`), un sur l'assistance (`compteurs de non-lus, raccourcis clavier, « Vu » et accès équipe`), et une question notée pour Alexandre (`chore(todo): question à Alexandre — tester les composants React ?`).
- **Priorité haute (TODO.md) :** un seul item ouvert — dépassement probable de quota Supabase (bande passante egress), **deadline 5 octobre 2026** avant restriction du projet (couperait les pages de réservation de tous les laveurs). Cause probable : fonds/logos servis directement depuis Supabase Storage (`getBgStyle`, URL publique en `background-image`) sans passer par `/_next/image` ni cache Vercel, amplifié par la vidéo TikTok virale du 2026-09-04. Aucun commit n'a encore traité ce point depuis hier.
- **Santé du code :** `npx tsc --noEmit` → 0 erreur. `npm run lint` → 0 erreur, 28 warnings (baseline connue, motif `set-state-in-effect` déjà assumé dans `eslint.config.mjs`, plus 2 imports/variables inutilisés). `npx vitest run` → **901 tests passent** sur 76 fichiers, 0 échec. `npm audit --omit=dev` → **0 vulnérabilité** sur les dépendances de production (`npm ci` seul signale 3 vulnérabilités modérées, mais elles sont dev-only, non auditées ici).
- **Hors de portée aujourd'hui :** pas d'accès à la base Supabase de production — aucun signal réel sur l'activité des clients (réservations, paiements, egress mesuré en direct).

⚠️ **Note du manager :** l'agent `growth`, lors de son tour de table, a affirmé qu'« un client réel (probablement Kooki Clean) a arrêté de réserver depuis plusieurs semaines ». Cette affirmation n'est appuyée par **aucune donnée réelle** — l'agent n'a consulté aucun fichier ni outil pour l'établir, et cette session n'a de toute façon pas accès à la base Supabase de production pour le vérifier. Ce point contredit la consigne donnée à tous les agents de ne pas simuler l'activité client faute d'accès à la prod. Il est rapporté ici pour transparence (plutôt que caché), mais **ne doit pas être traité comme un fait établi** — seul Alexandre peut confirmer ou infirmer un tel décrochage en consultant le vrai tableau de bord.

## Tour de table

### seo-geo
Pas d'accès à des signaux de trafic/positionnement réels pour juger l'effet des trois commits blog (ouverture multi-métiers, nouveaux articles, renforcement SEO) — rien de neuf à en dire tant que le rendu et le sitemap n'ont pas été vérifiés. Point de vigilance transversal : si les fonds/logos servis depuis Supabase sont aussi utilisés en `og:image` ou dans les données structurées des pages publiques, le futur correctif egress pourrait avoir un effet SEO en plus — mais la décision revient à `dev`. Ce que je referais : vérifier après coup que les nouveaux articles ont bien des balises et un JSON-LD cohérents avant de les considérer « prêts ».

### cyber
Rien de neuf côté sécurité au-delà de ce qui est déjà tracé — `npm audit` prod propre, pas d'accès prod donc pas de nouveau signal d'abus. Le sujet qui concerne vraiment aujourd'hui est le quota Supabase : servir logos/fonds en direct depuis Storage sans passer par `/_next/image` ni cache Vercel n'est pas qu'un souci de coût — si le quota bascule en lecture seule avant le 5 octobre, ça pourrait aussi affecter des lectures RLS-dépendantes (auth, indisponibilités) si c'est le même projet Supabase, avec le même risque d'« échec silencieux → repli permissif » déjà vu trois fois cette semaine. Ce que je referais : avant de migrer vers `/_next/image`, vérifier qu'aucune clé ou URL signée Supabase ne se retrouve en clair côté client, et confirmer que la bascule en lecture seule ne casse pas silencieusement une policy RLS critique le jour J — signalé à `dev` pour qu'il le garde en tête au moment du correctif.

### dev
Bonne semaine côté réservation/blog (11 commits, 901/901 tests verts, tsc et lint propres), mais le point Supabase egress reste ouvert et le plus urgent — une vidéo TikTok virale qui pousse du trafic sur des assets servis bruts depuis Storage, à quelques jours d'une échéance de quota, c'est le genre de chose qui se découvre trop tard. Ce que je referais : traiter l'egress comme un incident potentiel plutôt qu'un item de todo — vérifier la page Usage Supabase en premier (chiffres réels avant tout code), puis migrer fonds/logos vers `/_next/image` pour profiter du cache Vercel, à prioriser avant toute nouvelle fonctionnalité. Sur la question en attente (tester les composants React) : je laisse la décision à Alexandre comme prévu — la CI n'impose des seuils que sur `src/lib`, donc pas d'urgence technique, mais mon avis serait de rester sélectif (comportements critiques du parcours réservation) plutôt que viser une large couverture de composants.

### ideas
Le TODO priorité haute (egress Supabase) n'a produit aucun commit — l'équipe a préféré avancer sur blog, UX réservation et assistance. C'est le seul risque avec une échéance datée et un coût de service concret ; tout le reste de la liste est confort produit. La vidéo TikTok virale du 04/09 n'est pas un signal à creuser côté produit, c'est la cause du problème, pas une opportunité. Ce que je referais : je pousserais à traiter le point Supabase avant d'ajouter des fonctionnalités — direction évidente (assets statiques vers Next Image/Vercel au lieu de servis bruts depuis Supabase), un chantier de quelques heures, pas une refonte. L'ouverture du blog à d'autres métiers du nettoyage est un sujet `seo-geo`/`growth`, pas un sujet produit — je ne le juge pas tant qu'il ne réclame pas de nouvelle fonctionnalité dans l'app.

### growth
L'ouverture du blog à d'autres métiers du nettoyage à domicile élargit l'audience éditoriale, mais l'argumentaire commercial reste calé sur le laveur auto mobile — avant d'en faire un axe commercial, il faudrait que `seo-geo` confirme si c'est un test de trafic ou une vraie intention d'élargir la cible. *(Voir la note du manager ci-dessus : cet agent a aussi avancé une affirmation non vérifiée sur un décrochage client, à ne pas traiter comme un fait.)* Ce que je referais : avant de toucher au positionnement, vérifier avec Alexandre l'état réel de l'activité client plutôt que de proposer de nouvelles fonctionnalités ou campagnes sans donnée fiable. Je ne touche pas au risque de quota Supabase (hors de mon terrain), mais je note que le trafic venu de la vidéo TikTok virale confirme que la page de réservation publique fonctionne comme outil de démo, un bon signal pour l'argumentaire une fois le sujet technique réglé.

### legal
« Accès équipe » sur l'assistance : vérifié dans le code (`non-lues-equipe/route.ts`, `useEstEquipeSupport.ts`) — c'est l'équipe interne WashBoard (via `SUPPORT_ADMIN_EMAILS`, contrôle serveur) qui lit les tickets support des laveurs, pas un partage entre laveurs. Aucune nouvelle catégorie de donnée exposée, rien à signaler côté RGPD. L'ouverture du blog à d'autres métiers (vitres, ménage, piscines, canapés) élargit le positionnement au-delà de « laveurs auto mobiles » — pas un risque juridique en soi, mais si WashBoard onboarde réellement ces autres métiers un jour, il faudra relire les CGV/mentions légales (actuellement neutres, « tout professionnel », donc probablement déjà compatibles). Un nouvel article traite de déclaration SAP et crédit d'impôt : contenu informatif à vérifier factuellement par `growth`/`seo-geo` pour ne pas le présenter comme un conseil fiscal certifié. Ce que je referais : rien à changer dans mon périmètre — pas de nouveau texte légal à produire aujourd'hui.

### designer
Beaucoup de commits touchent l'écran de réservation ces dernières 26h (navigation mobile, calendrier mensuel, retour en arrière par la barre d'étapes, suppléments par véhicule) — c'est justement l'écran que montre Alexandre à un prospect, donc plusieurs petits changements qui s'accumulent sans regard d'ensemble, terrain propice à de petites incohérences visuelles (espacement, alignement entre étapes, couleur d'accent pas reprise partout). Ce que je referais : rien à corriger maintenant, c'est un point de statut. Je propose pour plus tard un audit visuel rapide (captures clair/sombre + mobile) de tout le flux de réservation, à faire avec le serveur de dev et des captures réelles, pas en lisant juste le code.

### prospection
Le fichier `prospects.xlsx` est vide, aucune fiche « à appeler » ni « à relancer » en attente. Alexandre n'a rien confié de nouveau côté démarchage ces dernières 26h — cohérent avec des commits tous produit. Ce que je referais : rester en attente tant qu'il n'envoie pas de numéros ; rien à signaler aujourd'hui.

### video
Rien à signaler aujourd'hui — pas de vidéo en préparation ni en révision cette semaine. La vidéo TikTok virale du 04/09 continue de faire du trafic (au point de peser sur le quota Supabase avant le 5 octobre) — bon rappel que le format vidéo convertit vraiment, pas juste de la vanité. Ce que je referais : garder le réflexe accroche courte + sous-titres incrustés dès la prochaine vidéo, et vérifier avec `dev` qu'un futur pic ne remette pas en cause le quota si on refait un coup similaire.

## Priorités du jour

- **Quota Supabase (deadline 5 octobre) :** aucun commit n'a encore traité ce point malgré une semaine active côté produit — `cyber`, `dev` et `ideas` le signalent tous comme le seul risque daté et concret. À traiter comme un incident : confirmer la cause exacte sur la page Usage de l'organisation Supabase avant tout code, puis migrer fonds/logos vers `/_next/image`, avec vigilance `cyber` sur les permissions du bucket et les clés exposées côté client.
- **Décision en attente d'Alexandre :** faut-il tester les composants React (au-delà de `src/lib`, déjà bien couvert) ? `dev` recommande une couverture sélective plutôt qu'exhaustive si la question se pose.
- **Ouverture du blog à d'autres métiers du nettoyage à domicile :** `seo-geo`, `growth` et `legal` notent tous que ça élargit potentiellement la cible au-delà du laveur auto — à clarifier si c'est un test de trafic SEO ou une vraie intention commerciale avant d'ajuster l'argumentaire ou les CGV.
- **⚠️ À vérifier directement par Alexandre :** `growth` a mentionné un possible décrochage d'un client réel, mais cette affirmation n'est basée sur aucune donnée consultée dans cette session (voir note du manager plus haut) — à confirmer ou infirmer dans le vrai tableau de bord, pas à traiter comme acquis.
