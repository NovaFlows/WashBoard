# Réunion d'équipe WashBoard — lundi 21 septembre 2026

## Signaux du jour

- **Commits (dernières 26h, 9 au total)** : grosse journée de refonte du tableau de bord
  laveur en plusieurs itérations rapprochées — réordonnancement des widgets, "Prochains
  rendez-vous" à la place de Factures, flèches de navigation sur "Aujourd'hui", raccourci
  Clients, couleur verte, effet au survol, chargement du calendrier rendu instantané. Au
  passage : fix identité (l'échantillon de couleur passait sous la palette), l'agent
  `video` a appris la méthode du "faux téléphone incrusté", et côté support : archivage
  de fil, menu retrouvé, réseaux sociaux ajoutés au menu.
- **TODO.md — Priorité haute** : un seul point réellement ouvert et urgent — **quota
  Supabase dépassé**, restriction des projets dès le **5 octobre 2026** si ça persiste
  (couperait les pages de réservation publiques de tous les laveurs). Cause probable :
  bande passante sortante (egress) — fonds/logos servis en direct depuis Supabase
  Storage à chaque visite d'une page de réservation, sans passer par l'optimisation
  d'images Next (`/_next/image`) ni le cache Vercel ; la vidéo TikTok virale du
  2026-09-04 a démultiplié les visites. Tout le reste de la section Priorité haute est
  coché/résolu.
- **Santé du code** : `npm ci` OK · `tsc --noEmit` propre (0 erreur) · `npm run lint` :
  0 erreur / **30 avertissements** (23 lors du dernier audit dette technique du 30/08 —
  légère hausse à surveiller) · `vitest run` : **951/951 tests verts** · `npm audit
  --omit=dev` : **0 vulnérabilité** en production (npm signale par ailleurs 3
  vulnérabilités modérées, mais dev-only).
- **Signal manquant** : pas d'accès à la base Supabase de production dans cette routine
  — rien à dire sur l'activité réelle des clients (réservations, paiements, trafic).

## Tour de table

### seo-geo
Le pic egress vient probablement du trafic TikTok du 4/09 sur la page de réservation
publique (logos/fonds servis en direct depuis Supabase Storage plutôt que via
`/_next/image`) — sujet infra plus que SEO, mais si cette vidéo mentionne WashBoard
nommément ça vaudrait la peine de le vérifier côté growth (mention/backlink). Rien de
neuf côté blog/sitemap/JSON-LD aujourd'hui. Ce qu'il referait : rester en veille et
transmettre le point technique à `dev`, la piste mention à `growth`.

### cyber
Rien de sécurité neuf dans les commits du jour. Le point qui compte : le quota
Supabase, urgent avant le 5 octobre car il coupe les pages de réservation publiques de
tous les laveurs — un sujet de disponibilité, pas une fuite de données, mais à traiter
avec la même urgence qu'une faille. Hygiène par ailleurs bonne (tsc/lint/tests propres,
0 vulnérabilité prod). Ce qu'il referait : continuer à surveiller les `?? []` et catchs
silencieux à chaque nouvelle route, et demander à `dev` de tracer/logguer les erreurs
d'egress pendant la correction, pour avoir un vrai signal en cas de récidive.

### dev
Gros rythme sur le dashboard hier (9 commits, essentiellement du polish widgets/nav),
mais le vrai sujet urgent est ailleurs : egress Supabase probablement dû à `getBgStyle`
qui sert fonds/logos en `background-image` direct plutôt que via `/_next/image`, avec
restriction dès le 5 octobre. Code sain par ailleurs, lint en légère hausse (30 vs 23)
à surveiller. Ce qu'il referait : geler les itérations dashboard un jour et traiter le
quota Supabase en premier — confirmer le poste de coût exact dans Usage Supabase avant
de migrer `getBgStyle`, et vérifier que la migration ne casse pas les fonds custom par
laveur avant de pousser.

### ideas
La refonte dashboard (widgets réordonnables, "Prochains rendez-vous" au lieu de
Factures) est cohérente avec le pitch : de la gestion visible au quotidien pour un
laveur seul, rien à tuer à vue de statut. Le vrai signal du jour, c'est le quota
Supabase — pas un sujet à juger, un incident avec échéance connue et cause identifiée,
qui touche au cœur du produit (la page de réservation publique) : à passer à `dev` en
priorité, pas à débattre en réunion. Ce qu'il referait : noter qu'on juge une refonte
dashboard sans donnée d'usage réel (pas d'accès Supabase prod) — pas bloquant ici, mais
à garder en tête pour la prochaine décision plus structurante.

### growth
Le vrai signal urgent n'est pas produit mais le quota Supabase : si la page de
réservation publique est coupée au 5 octobre, ça coupe le meilleur outil de démo
commerciale (Kooki Clean inclus) en pleine refonte dashboard. Sans accès Supabase prod,
impossible de dire si le pic TikTok viral du 04/09 a généré des réservations ou juste
de l'egress — aucune conclusion commerciale tirée tant que ce n'est pas confirmé. Ce
qu'il referait : ne rien inventer sur l'usage réel, laisser le sujet quota à `dev` /
Alexandre, continuer de vérifier le client silencieux plutôt que lancer une nouvelle
campagne.

### legal
Rien de neuf côté juridique/conformité aujourd'hui : les commits (dashboard, identité,
agent video, support) ne touchent ni collecte de données nouvelle ni texte légal.
Remarque : si la solution au quota Supabase implique de changer d'infra dans l'urgence,
ça vaudrait la peine de relire `legal`/`cyber` avant bascule, pas après. Rien à rouvrir
sur le report légal/Stripe à mi-novembre ni sur le PDF de facture public en noindex —
tranché, pas de nouveau signal.

### designer
Pas de capture prise, jugement uniquement sur les messages de commit : mouvement
cohérent (Factures dehors, Prochains rendez-vous dedans, navigation calendrier plus
réactive, raccourci Clients) et un fix d'identité (échantillon de couleur sous la
palette) qui touche direct au mécanisme d'accent par laveur, à vérifier en premier dès
que les pixels seront disponibles. Ce qu'il referait : demander une capture à la fin
d'une série d'itérations aussi rapprochées plutôt qu'après plusieurs jours — sans ça,
impossible d'auditer la cohérence du résultat final. Point de vigilance hors-scope : si
la solution au quota Supabase passe par de l'optimisation d'images, vouloir être dans
la boucle pour éviter une dégradation visuelle sur l'outil de démo commerciale.

### prospection
Rien à signaler aujourd'hui — aucun nouveau numéro reçu, rien à enrichir dans le
fichier.

### video
Le commit 8ba89ab documente la méthode du "faux téléphone incrusté" (ffmpeg +
Playwright, atelier `scratch_video`, dalle 211×456 à l'offset 24,24) — la méthode qui a
servi aux vidéos 2 à 4, rien de neuf à livrer, juste un gain de temps pour la prochaine
vidéo TikTok avec écran de produit incrusté. Trois règles à garder : composer les
transitions sur la dalle avant de la poser dans le châssis, aucune ombre portée,
téléphone immobile une fois posé. Rien d'autre à signaler, aucune tâche en cours ni en
attente.

## Priorités du jour

- **Quota Supabase (avant le 5 octobre)** : signal remonté indépendamment par cyber, dev,
  ideas, growth, designer et seo-geo — c'est le seul sujet unanime de la réunion. À
  traiter maintenant (confirmer le poste de coût dans Usage Supabase, migrer
  `getBgStyle` vers `/_next/image`) plutôt que la semaine du 5 octobre, sans casser les
  fonds personnalisés par laveur sur la page de réservation publique.
- **Lint en légère hausse** (30 avertissements contre 23 au dernier audit dette
  technique du 30/08) — à surveiller, sans urgence.
- **Traçabilité de la refonte dashboard** : plusieurs commits rapprochés sur la même
  zone en une seule journée, sans capture intermédiaire (remarque de `designer`) — à
  documenter par une capture avant/après la prochaine fois.
- **Aucune donnée d'usage réel** pour juger la refonte dashboard ou l'impact du pic
  TikTok viral (accès Supabase prod hors de portée de cette routine) — noté par
  `ideas` et `growth`, pas bloquant mais à garder en tête.
