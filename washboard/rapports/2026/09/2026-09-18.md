# Réunion d'équipe WashBoard — vendredi 18 septembre 2026

## Signaux du jour

- **13 commits** dans les dernières 26h : fonctionnalité "assistance" (le laveur peut poser une question à l'équipe depuis le dashboard), centre d'aide (tri par pertinence + mise à jour de 3 entrées), traçage de cinq lectures qui échouaient en silence, un raté de lecture qui ne déconnecte plus le laveur, notification d'équipe sur paiement Stripe reçu/refusé, interdiction d'indexation du PDF de facture public, perf dashboard (deux attentes réseau supprimées par page), et le rapport de la réunion d'hier.
- **Santé du code** : `tsc --noEmit` propre (0 erreur), ESLint 26 warnings / 0 erreur (motifs déjà connus : `react-hooks/set-state-in-effect` sur des mount-guards, `no-img-element` sur des logos/fonds), Vitest **856/856 tests verts** (72 fichiers).
- **`npm audit --omit=dev`** : **0 vulnérabilité**. (Sur l'ensemble du projet, dev inclus, `npm ci` remonte 3 vulnérabilités modérées, toutes en dev uniquement.)
- **Priorité haute (TODO.md)** : le point encore ouvert est le **quota Supabase dépassé** — restriction des projets prévue à partir du **5 octobre 2026** si le dépassement n'est pas réglé. Cause probable : fonds/logos servis en direct depuis Supabase Storage (`getBgStyle`) sans cache Vercel, aggravé par une vidéo TikTok virale du 04/09. Reste à faire : confirmer la cause exacte sur la page Usage Supabase, migrer vers `/_next/image`, vérifier les autres projets de l'organisation, éventuellement passer en offre Pro avant l'échéance.
- **Signal manquant** : pas d'accès à la base Supabase de production — aucune information sur l'activité réelle des clients (réservations, paiements, trafic réel) dans ce rapport.

## Tour de table

### seo-geo
Point de vigilance : si Vercel restreint l'accès aux assets Supabase début octobre, ça peut dégrader le LCP des pages publiques et les Core Web Vitals — pas urgent aujourd'hui, à surveiller une fois le correctif dev posé. Rien de neuf côté contenu ou balises. Pas de données de trafic réel disponibles.

### cyber
La série de correctifs sur les lectures silencieuses est exactement le motif surveillé — bon signe que ce soit traité en masse. Le garde-fou "client historique" écarté par Alexandre est une décision produit assumée, pas un oubli : rien à challenger tant que le flag `active` est posé de façon atomique. Le quota Supabase est un sujet de disponibilité, pas de sécurité — à router vers dev/infra. Rien de neuf côté secrets/RLS. Recommande que chaque "lecture silencieuse" corrigée porte un test qui vérifie la trace, pas juste l'absence de crash.

### dev
Le quota Supabase reste la priorité — il faut trancher vite entre migration `/_next/image` et passage en offre Pro avant le 5 octobre, la marge de manœuvre se resserre. Bon signe côté hygiène : les cinq lectures muettes tracées, et la double logique de clôture (`BookingList.tsx` vs `lib/cloture.ts`) enfin documentée en TODO plutôt que rustinée en douce. Pousse pour qu'Alexandre tranche ce doublon avant la prochaine régression — c'est le même mécanisme qui avait laissé le calendrier sans protection.

### ideas
La vidéo TikTok virale qui a fait sauter le quota Supabase est le vrai signal du jour : une preuve de traction commerciale bien plus parlante qu'une idée inventée sans données. Reste un sujet infra/perf, pas produit. Rien de neuf à proposer tant qu'il n'y a pas de données d'usage réel (adoption assistance, conversion réservation) — pas d'idée inventée pour combler ce manque.

### growth
Attention à ne pas laisser la nouvelle notif Stripe "paiement reçu" fuiter dans une démo — Stripe n'est pas encore en service, tout paiement reste manuel (PayPal/virement). Le quota Supabase menace la page de réservation, le principal outil de démo commercial : si elle saute le 5 octobre, ça touche l'argumentaire de vente directement. Flague qu'avant toute campagne d'acquisition supplémentaire, il faut que l'egress soit réglé — sinon on amplifie le problème plutôt que la conversion. Pas de données de conversion réelles disponibles.

### legal
Le lien PDF de facture public + noindex est une rustine techniquement correcte, mais reste un choix de risque résiduel RGPD assumé par Alexandre — mériterait d'être documenté (registre de traitement) plutôt que de rester une décision orale. Rien d'autre de neuf : société toujours pas créée, légal et Stripe live toujours repoussés à mi-novembre, cohérent avec ce qui était déjà acté.

### designer
Rien de neuf visuellement sur les commits du jour (assistance et centre d'aide sont du contenu/logique, pas du design). Seul point pertinent : les `<img>` non optimisées dans `IdentiteForm.tsx` (logos/fonds) sont un candidat plausible au problème de quota Supabase, mais c'est à dev de confirmer via les tailles réellement servies avant tout correctif visuel. Suggère de signaler ce lien à dev dès maintenant plutôt que d'attendre une session dédiée.

### prospection
Rien à signaler aujourd'hui — aucun numéro confié par Alexandre depuis la dernière réunion, pas d'accès prod donc pas de prospects réels à qualifier. Fichier prospects inchangé, en attente de nouveaux contacts.

### video
Rien de neuf — les 13 commits sont tous produit/code, aucun impact sur des montages en cours. Note le point Supabase/vidéo virale du 04/09 comme contexte, sans action de sa part. Aucune voix-off générée, aucun rendu lancé.

## Priorités du jour

- **Quota Supabase (échéance 5 octobre 2026)** : c'est le sujet unanime — dev, growth, seo-geo, designer et ideas le mentionnent tous. À trancher vite : confirmer la cause exacte (page Usage Supabase), migrer les images vers `/_next/image`, et décider si on passe en offre Pro en attendant. Impact direct sur la page de réservation, outil de démo commerciale.
- **Doublon de la règle de clôture** (`BookingList.tsx` / `lib/cloture.ts`) : signalé par dev et déjà noté au TODO — à trancher par Alexandre avant qu'une nouvelle régression ne reproduise le bug déjà corrigé sur le calendrier.
- **Notification Stripe "paiement reçu"** livrée mais Stripe pas encore en service (growth) : vigilance à ne pas montrer cet écran comme un automatisme actif en démo ou onboarding.
- **Lien PDF de facture public** : rustine noindex en place, mais legal recommande de documenter ce risque résiduel RGPD plutôt que de le laisser en décision orale.
