# Réunion d'équipe WashBoard — jeudi 10 septembre 2026

## Signaux du jour

- **3 commits** dans les dernières 26h :
  - `fix(landing)` — retrait de la mention « multi-véhicules » du plan Essentiel (incohérente avec le positionnement élargi à tout type de laveur).
  - `fix(prestations)` — un type de prestation « sur devis » à 0€ faussait l'affichage « à partir de X€ » sur la carte prestation ; signalé sur le vrai compte **Kookii Clean** (prestation Lavage Textile, type Tapis à 0€). Corrigé dans `lib/pricing.ts` (`minVehiclePrice` exclut désormais les types à 0€), test ajouté.
  - Le rapport d'hier (2026-09-09).
- **Section 🔴 Priorité haute du TODO** — 2 points encore ouverts :
  1. **BellAuto89** (inscrit le 2026-08-20, essai jusqu'au 2026-09-19) : a tout configuré (logo, adresse, 28 créneaux, 2 catégories) sauf ses prestations → 0 prestation, 0 réservation possible, page publique vide. N'est pas venu à un rendez-vous convenu avec Alexandre. Le TODO pose l'hypothèse que l'écran Prestations bloque le laveur.
  2. **Action requise d'Alexandre** : créer les 7 secrets GitHub (`E2E_SUPABASE_URL`, `E2E_SUPABASE_ANON_KEY`, `E2E_SUPABASE_SERVICE_ROLE_KEY`, `E2E_WASHER_EMAIL`, `E2E_WASHER_PASSWORD`, `E2E_WASHER_SLUG`, `E2E_CLIENT_EMAIL`) pour que le job e2e de la CI tourne réellement — aujourd'hui il s'arrête proprement sans rien tester.
- **Santé du code** : `npm ci` OK · `tsc --noEmit` propre (0 erreur) · `lint` 0 erreur / 23 warnings (motif `set-state-in-effect` assumé, inchangé) · `vitest run` **621/621 tests** verts · `npm audit --omit=dev` **0 vulnérabilité** (l'audit complet avec les dépendances de dev en trouve 3 modérées, non exposées en prod).
- **Angle mort assumé** : pas d'accès à la base Supabase de production depuis cette routine (aucun secret dans le dépôt) → impossible de dire quoi que ce soit sur l'activité réelle des clients (réservations, trafic, conversions) aujourd'hui.

## Tour de table

### seo-geo
Rien de neuf : les 3 commits touchent la landing (cohérence de positionnement) et l'affichage d'un prix sur une carte prestation — aucun impact sur balises, contenu blog ou données structurées. Point de vigilance pour un prochain audit : vérifier que le même écueil (« à partir de 0€ ») ne traîne pas côté marketing (article, méta-description) si un prix y est cité, pour rester aligné avec `PLAN_CARDS`.

### cyber
Rien à signaler : le fix `pricing.ts` est un bug d'affichage pur, sans autorisation ni donnée sensible en jeu, et `npm audit --omit=dev` reste à 0. Seul point de vigilance non urgent, déjà identifié : tant que les 7 secrets GitHub ne sont pas posés, la CI n'a aucune couverture e2e réelle — pas de filet si une régression touchait l'auth ou les routes API. Pas de RLS/table touchée cette semaine, pas d'audit approfondi à relancer aujourd'hui.

### dev
Rien de cassé : typecheck/lint/tests/audit propres, et le fix `minVehiclePrice` est le genre de correctif attendu — bug réel remonté par un vrai client, isolé, testé. Sur BellAuto89 : ça sent plus un problème produit/UX (bouton désactivé sans message d'erreur explicite) qu'un bug technique — pas à trancher seul, ça mérite un passage par `designer` pour le pattern d'erreur du formulaire Prestations avant de coder quoi que ce soit. Les 7 secrets GitHub restent côté Alexandre.

### ideas
Vérification faite sur le code actuel de `PrestationsManager.tsx` : **l'hypothèse du TODO ne tient pas**. `canSave` n'exige ni catégorie ni type coché (nom + prix + durée suffisent), et l'API `POST /api/services` accepte `category_id: null` et `vehicle_types: []`. De plus, un commit du 2026-09-04 (`380796a`, après le constat BellAuto89 du 08-31) a déjà changé le comportement : tous les types d'une catégorie sont désormais cochés par défaut à l'ouverture du formulaire, précisément pour éviter qu'un laveur enregistre une prestation sans aucun type sélectionnable. La piste « écran Prestations bloquant » telle que formulée n'est donc pas confirmée par le code — inutile de la faire juger par `designer` en l'état. Manque de signal réel pour trancher entre simple interruption et friction ailleurs dans l'onboarding : rien n'instrumente aujourd'hui le tunnel de configuration laveur (logo → créneaux → catégories → prestations), contrairement au tunnel de réservation client. Avant toute nouvelle hypothèse, un événement minimal sur ces étapes serait plus fiable qu'une nouvelle supposition.

### growth
Le cas BellAuto89 reste le signal le plus parlant : quelqu'un qui a fait tout le travail de setup et bute sur la toute dernière étape, ce qui l'a empêché de voir le produit fonctionner avant même de manquer son rendez-vous. Ce n'est pas un sujet d'argumentaire commercial mais de déblocage : recommandation d'une relance courte et non commerciale (« il te manque juste tes prestations, 5 min, je peux t'aider ») plutôt que l'accroche prospect générique déjà préparée. Rien à signaler sur le reste (fix de prix = sujet dev, pas de données réelles de réservation/trafic aujourd'hui).

## Priorités du jour

- **BellAuto89** : l'hypothèse « écran Prestations bloquant » ne résiste pas à la lecture du code actuel (`ideas`) — le sujet reste ouvert mais change de nature : investiguer plutôt une interruption ponctuelle ou une friction ailleurs dans l'onboarding laveur, à vérifier par l'appel prévu. Angle de relance suggéré par `growth` : déblocage, pas vente.
- **7 secrets GitHub pour la CI e2e** : toujours en attente côté Alexandre — sans eux, aucune régression auth/API n'est couverte par les tests e2e.
- Aucun signal de dette de code nouveau aujourd'hui (typecheck/lint/tests/audit tous propres) — rien à traiter dans l'urgence côté technique.
