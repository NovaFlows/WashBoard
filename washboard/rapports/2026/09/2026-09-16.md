# Réunion d'équipe WashBoard — mercredi 16 septembre 2026

## Signaux du jour

- **Commits (dernières 26h) :** rythme soutenu — nouvel onglet Clients au dashboard (fichier clients avec recherche), import de factures, flux de « Clôture » d'un créneau passé (confirmation « le rendez-vous a-t-il eu lieu ? »), notification du soir plus visible pour clôturer les rendez-vous du jour, réseaux sociaux ajoutés au pied de page de la landing et dans le JSON-LD (`sameAs`), mise à jour du centre d'aide, plusieurs correctifs (affichage mobile du dashboard, casse des filtres factures, lecture des réservations au-delà de 1000, tâches attribuées à Ryan dans le TODO).
- **TODO — Priorité haute :** un seul point ouvert. Alerte Supabase : quota d'organisation dépassé, restriction des projets annoncée à partir du **5 octobre 2026** si rien ne change. Cause probable identifiée : bande passante sortante (egress) — les fonds/logos des pages de réservation sont servis directement depuis Supabase Storage, sans passer par l'optimisation d'images Next (`/_next/image`) ni le cache Vercel. Une vidéo TikTok devenue virale le 4 septembre a fait grimper le trafic et révélé le problème. Pas encore corrigé.
- **Santé du code :** `npx tsc --noEmit` propre (0 erreur). `npm run lint` : 0 erreur, 22 avertissements (tous du même pattern `set-state-in-effect`, connu et accepté dans `eslint.config.mjs`). `npx vitest run` : **774/774 tests passent** (61 fichiers). `npm audit --omit=dev` : **0 vulnérabilité** en dépendances de production (3 vulnérabilités modérées existent, mais uniquement côté devDependencies).
- **Angle mort assumé :** pas d'accès à la base Supabase de production depuis cette session — aucune donnée sur l'activité réelle des clients (réservations, paiements, conversions) n'a pu être vérifiée aujourd'hui.

## Tour de table

### seo-geo
Le `sameAs` (Instagram + TikTok) ajouté hier est propre : documenté, confirmé par Alexandre, testé pour ne pas casser la contrainte « tout pointe vers washboard.fr ». Le vrai signal qui le concerne est l'alerte Supabase : si le quota force une restriction au 5 octobre, les pages de réservation publiques que Google indexe pourraient tomber en erreur (risque Core Web Vitals / désindexation). Ce n'est pas son terrain technique mais l'échéance est proche, donc il le signale. Rien à publier ni modifier côté contenu aujourd'hui.

### cyber
Le nouvel onglet Clients suit le bon réflexe post-correctifs de la semaine : scoping strict par `washer_id`, erreurs de lecture tracées au lieu d'être avalées silencieusement, colonnes limitées (pas de notes internes ni contenu de factures exposés). L'import de factures (parsing d'un PDF fourni par l'utilisateur) n'a pas encore été relu en détail côté sécurité — prévu avant que la partie « factures d'achat » sorte du statut « en développement ». L'alerte Supabase egress est un risque de disponibilité, pas de confidentialité, mais reste priorité haute.

### dev
Rien de cassé côté code : typecheck clean, lint 0 erreur, 774/774 tests verts, 0 vulnérabilité en prod. Le rythme de commits des dernières 26h est sain, sans dette apparente ajoutée. La seule chose qui compte vraiment aujourd'hui est l'egress Supabase (échéance datée et mesurable, prime sur toute nouvelle fonctionnalité) : proposition de basculer les fonds/logos vers `next/image` (chantier d'un à deux jours), avec validation de `cyber` avant d'élargir les origines d'images autorisées.

### ideas
Constat le plus concret du jour : le rapprochement entre le flux de clôture et la notification du soir révèle une vraie faille — la confirmation « le rendez-vous a-t-il eu lieu ? » ne protège que `/dashboard`, pas `/dashboard/calendrier`, et c'est justement vers ce calendrier non protégé que pointe la notification de 22h. Un laveur pourrait clôturer et facturer d'un clic un rendez-vous jamais confirmé — touche à l'intégrité de la facturation. Deux pistes posées pour Alexandre : correctif rapide (repointer la notification) vs. parité complète dans le calendrier (chantier plus lourd, sans test). Pas de nouvelle idée produit proposée tant que ce point n'est pas tranché.

### growth
Le pic TikTok du 4 septembre est un signal commercial rare, mais sans donnée de conversion disponible il est impossible de savoir s'il a généré des laveurs réels ou juste des vues — pas de campagne à construire dessus pour l'instant. L'onglet Clients et le flux de clôture touchent directement la rétention, mais là aussi sans visibilité sur l'usage réel. Recommandation : demander à Alexandre l'état d'usage réel (réservations actives) avant toute idée d'acquisition autour du buzz.

### legal
Rien de neuf à documenter aujourd'hui : ni l'onglet Clients ni l'import de factures ne collectent de donnée nouvelle — même finalité (exécution des réservations), colonnes limitées, stockage privé avec lien signé à durée courte pour les factures importées. Point de vigilance pour plus tard : si l'onglet Clients gagne un jour une fonction d'export ou de relance groupée, ce serait un changement de finalité à faire valider avant que `dev`/`growth` avancent. Si la solution au quota Supabase passe par un service hors UE, vérifier les transferts de données.

### designer
Nuit chargée côté affichage mobile (en-tête du dashboard qui tient sur petit écran, logo retiré du header) — bon sens vu l'usage terrain du laveur. Le renommage de « Factures » en « Ventes | Achats » et le nouvel onglet Clients changent la façon dont l'écran se lit ; à vérifier au prochain passage que les libellés tiennent sur mobile. Rien à corriger dans l'urgence aujourd'hui ; prochaine session prévue pour capturer les écrans retouchés cette nuit.

### prospection
Rien à signaler aujourd'hui : le fichier de prospects est vide, aucun numéro reçu dans les dernières 26h.

### video
Rien à signaler aujourd'hui : aucune nouvelle commande reçue. Simple observation : le succès du TikTok du 4 septembre valide le format actuel (sobre, sous-titres incrustés) pour convertir vers la page de réservation.

## Priorités du jour

- **Egress Supabase (échéance 5 octobre)** : servir les fonds/logos via `next/image` + cache Vercel plutôt que directement depuis Supabase Storage — signalé indépendamment par `dev` et `seo-geo` comme la priorité du jour.
- **Faille du flux de clôture** (`ideas`) : la notification du soir pointe vers `/dashboard/calendrier`, qui n'a pas le garde-fou « le rendez-vous a-t-il eu lieu ? » présent sur `/dashboard` — un laveur peut clôturer/facturer sans confirmation. À trancher par Alexandre entre correctif rapide et refonte du calendrier.
- **Revue sécurité de l'import de factures** encore à faire (`cyber`) avant que la partie « factures d'achat » sorte du statut « en développement ».
- **Angle mort persistant** : aucune donnée d'usage réel (réservations, conversions, clients actifs) disponible pour l'équipe — `growth` et `designer` en ont besoin pour prioriser correctement.
