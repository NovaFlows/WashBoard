# Réunion d'équipe WashBoard — jeudi 24 septembre 2026

## Signaux du jour

- **Commits (dernières 26h, 4) :** merge de synchronisation ; `feat(landing): ajouter la facturation et un mot sur la reforme 2027` ; `perf(vercel): fonctions à Paris (cdg1), à côté de la base Supabase` ; `chore(rapport): reunion d'equipe du 2026-09-23`.
- **TODO — Priorité haute :** 2 points ouverts, 1 clos depuis hier.
  - Ouvert : **quota Supabase dépassé** — restriction des projets possible dès le 5 octobre 2026 si l'organisation reste au-dessus du quota. Cause probable : bande passante sortante (egress), les fonds/logos des laveurs étant servis directement depuis l'URL publique Supabase (`getBgStyle`) sans passer par le cache Vercel, avec un pic dû à une vidéo TikTok virale du 4 septembre.
  - Ouvert : **audit post-lancement du 21/09** (checklist 20 points, fait par Ryan) — 15/20 déjà en place, 5 restent : vitesse (page de réservation LCP 3,1 s / TTFB 2,3 s, accueil CLS 0,155), contraste des couleurs (WCAG AA en échec sur plusieurs pages), pas d'og:image/twitter:image sur l'accueil, pas de page CGU, pas d'acceptation CGV/CGU tracée à l'inscription.
  - Clos : « L'écran Prestations piège les nouveaux inscrits » (relevé précédemment par `ideas`) est maintenant coché comme fait.
- **Santé du code :** `tsc --noEmit` propre (0 erreur). `npm run lint` : 0 erreur, 30 warnings (surtout `react-hooks/set-state-in-effect`, plus deux imports/variables inutilisés). `npx vitest run` : 84 fichiers, **1001 tests, tous passent**. `npm audit --omit=dev` : **0 vulnérabilité** en production (3 vulnérabilités modérées signalées en dépendances de dev lors de `npm ci`, hors scope prod).
- ⚠️ **Signal manquant :** aucun accès à la base Supabase de production dans cette routine → rien à dire sur l'activité réelle des clients (réservations, paiements). Idem pour Vercel Analytics : aucun token configuré.

## Tour de table

### seo-geo
Confirmé dans `src/app/layout.tsx` : les blocs `openGraph` et `twitter` de la page d'accueil n'ont toujours pas de champ `images` — le point 3 de l'audit Ryan est à jour, et c'est justement la page qui hérite du `card: summary_large_image`. Rien à signaler côté commits d'hier sur ses balises.
Ce qu'il referait : pousser ce correctif en tête de liste — une simple image à référencer dans les deux blocs, cinq minutes de travail, débloque l'aperçu du lien le plus partagé (contrairement au blog/réservation déjà couverts). Sans données de trafic, impossible de chiffrer l'impact, mais le meilleur ratio effort/résultat du jour.

### cyber
Le quota Supabase (egress via `background-image` non caché) est le vrai sujet à traiter en premier — fenêtre courte avant le 5 octobre ; ce n'est pas une faille en soi, mais à vérifier qu'aucun contournement ne change l'exposition des fichiers. Sur le point CGV/CGU de `/signup` : recommande un stockage explicite (version des CGU acceptée + horodatage + user_id) plutôt qu'une simple case côté front, pour rester vérifiable en cas de litige — à croiser avec `legal` pour le texte. Rien de neuf côté dépendances/tests (0 vulnérabilité prod, tsc/lint/tests propres).
Ce qu'il referait : rien à corriger lui-même (point de statut) ; transmettra son avis à `legal`/`dev` quand ils attaqueront le sujet CGU, et revérifiera le quota Supabase après correctif.

### dev
Le commit landing (facturation/réforme 2027) touche du contenu public, hors de son rayon. Le déplacement des fonctions Vercel à `cdg1` va dans le bon sens pour le TTFB, mais ne réglera pas seul le LCP ni le CLS si les fonds/logos continuent d'être servis en direct depuis Supabase — ce qui alimente aussi le quota egress qui saute au 5 octobre.
Ce qu'il referait : sortir `getBgStyle` du chemin Supabase direct pour passer par `next/image`/cache Vercel — un seul chantier pour deux items de la TODO (quota + LCP/CLS). Pour la case CGV/CGU, il codera la mécanique mais fera relire le format par `legal` et `cyber` avant de merger, comme demandé par l'audit.

### ideas
Le commit "facturation" sur la landing mérite une vérification rapide : WashBoard exclut le paiement en ligne du V1, donc si "facturation" dépasse l'argumentaire texte, il faut clarifier avant que ça s'installe comme fonctionnalité. Bon signal sur l'écran Prestations désormais coché, mais sans accès prod impossible de le confirmer par l'usage réel.
Ce qu'il referait : pas de nouvelle idée proposée aujourd'hui, faute de signal produit fiable. Le seul point qui mérite la priorité reste le quota Supabase (échéance 5 octobre) — un risque de service, pas un sujet produit, mais rien ne sert d'ajouter des fonctionnalités tant que ce n'est pas traité.

### growth
Le commit facturation/réforme 2027 est cohérent avec la TODO (Stripe live repoussé mi-novembre, activation manuelle en attendant) — l'argumentaire d'onboarding reste inchangé. Note technique à vérifier avec Alexandre : `lib/plan.ts` affiche aujourd'hui « 1 mois offert » sur l'engagement annuel, à recouper avec le discours commercial en circulation s'il mentionne encore « 2 mois offerts ».
Ce qu'il referait : avant toute idée de campagne, redemander directement à Alexandre où en est Kooki Clean plutôt que de proposer une idée d'acquisition à l'aveugle.

> ⚠️ **Note du manager :** `growth` a formulé cette dernière phrase en affirmant que Kooki Clean « semble avoir arrêté de réserver » — affirmation qui n'est appuyée par aucune donnée réelle (aucun accès Supabase prod dans cette routine, et aucun fichier du dépôt ne documente une activité client). Le même agent avait déjà fait une affirmation quasi identique et tout aussi injustifiée lors de la réunion du 20/09 (déjà signalée à l'époque). Ce point est rapporté pour transparence mais **ne doit pas être traité comme un fait** — seul Alexandre peut vérifier l'activité réelle de Kooki Clean dans le vrai tableau de bord. Il pourrait valoir la peine de revoir le prompt de `growth` pour couper ce réflexe.

### legal
Deux trous d'opposabilité juridique réels : pas de page CGU, et `/signup` ne fait signer ni CGU ni CGV (aucune trace horodatée) — affaiblit WashBoard en cas de litige avec un laveur. Le rappel de la mesure d'audience manque dans `/confidentialite` (léger, à ajouter). Sur la landing, la mention de la réforme facturation électronique 2027 est présentée comme acquise avec un engagement commercial ferme ("on te raccordera avant l'échéance") — le calendrier légal a déjà bougé par le passé, ce n'est pas neutre de l'affirmer aussi fermement.
Ce qu'il referait : rédiger le texte CGU et l'associer aux CGV en une seule case à cocher horodatée (spec pour `dev`, relecture `cyber` avant merge, rien ne part sans validation d'Alexandre) ; nuancer la phrase sur 2027 ("s'en occupe" → "prévoit de s'en occuper") en confirmant le calendrier officiel avant de le republier comme certain.

### designer
Le contraste casse en plusieurs endroits en mode clair — pied de page, dates d'articles, mentions légales en `text-slate-400` trop pâles (surtout au soleil sur mobile), onglet actif de la réservation quasi invisible. La page d'accueil, pourtant le lien le plus partagé, n'a pas d'image de partage (og:image) — elle apparaît sans visuel sur LinkedIn/WhatsApp, contrairement au blog et aux pages de réservation.
Ce qu'il referait : foncer les gris clairs et l'onglet actif pour rester lisible sans changer la palette de base, et préparer une image de partage pour l'accueil cohérente avec celles déjà en place — avec captures avant/après à l'appui.

### prospection
Rien à signaler aujourd'hui — aucun fichier de prospects sur le disque et aucun numéro confié depuis la dernière réunion. Le script est prêt mais attend qu'Alexandre envoie des numéros.

### video
Rien de neuf — pas de nouvelle demande d'Alexandre. Note en passant : la vidéo TikTok virale du 4 septembre (qui a fait grimper le trafic réservation et pèse sur le quota Supabase) est un sujet infra pour `dev`, pas une commande vidéo. Aucune voix-off générée, aucun rendu lancé, conformément à la consigne de cette réunion de statut.

### sentry
Rien à signaler aujourd'hui — aucune alerte, `errorId` ni `digest` en attente. Sentry est configuré dans le code (`sentry.server.config.ts`, `sentry.edge.config.ts`, instrumentation) mais le DSN n'est pas encore renseigné ; la TODO confirme que le error tracking reste une tâche à activer avant/juste après le lancement, pas encore active.
Ce qu'il referait : tant que le DSN n'est pas branché, toute alerte repose sur les logs Vercel manuels — pas de notification proactive si un laveur ne signale rien.

### analytics
Pas d'accès configuré aujourd'hui (ni token Vercel, ni accès Supabase prod dans cet environnement) — rien de notable à rapporter côté trafic. Prêt à analyser une capture Vercel Analytics ou un export CRM si Alexandre en partage un.

## Priorités du jour

- **Quota Supabase avant le 5 octobre** : sujet consensuel (`cyber`, `dev`, `ideas`) — servir fonds/logos via `next/image`/cache Vercel plutôt que directement depuis Supabase ; ce même chantier améliore aussi le LCP/CLS de l'audit post-lancement.
- **CGU + acceptation CGV/CGU à l'inscription** : `legal` rédige le texte, `dev` implémente une case horodatée, `cyber` relit avant merge — les trois s'accordent sur la marche à suivre.
- **og:image manquante sur l'accueil** : petit correctif à fort impact (`seo-geo`, `designer` s'accordent), la page la plus partagée n'a aucun visuel d'aperçu.
- **Contraste des couleurs en mode clair** (`designer`) à corriger sans toucher à la palette de base.
- **Vigilance sur le contenu commercial** : `ideas` demande de clarifier ce que "facturation" recouvre sur la landing (pas de paiement en ligne en V1) ; `legal` demande de nuancer l'engagement ferme sur la réforme 2027 ; `growth` signale un possible écart "1 mois" vs "2 mois offerts" dans le discours commercial — trois points de cohérence à trancher avec Alexandre, pas des urgences techniques.
