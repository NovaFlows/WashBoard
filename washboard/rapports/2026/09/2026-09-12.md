# Réunion d'équipe WashBoard — samedi 12 septembre 2026

## Signaux du jour

- **9 commits** dans les dernières 26h : rebranding logo (3 commits — nouveau logo sur le site + essais d'icône d'application), retrait du repli sur l'ancienne variable d'env de la clé Google Maps (la migration Vercel avait été vérifiée la veille), ajout au health check du nom de variable Maps effectivement utilisé, une note TODO sur la reprise automatique d'aperçu à l'inscription, un fix inscription (un prospect peut s'inscrire avec le numéro de téléphone de son aperçu commercial), un fix fuseau horaire (emails/notifications affichaient l'heure UTC au lieu de l'heure de Paris), et le commit du rapport d'hier.
- **Section 🔴 Priorité haute du TODO** — 1 point encore ouvert de fond :
  **BellAuto89** (inscrit le 2026-08-20, essai jusqu'au 2026-09-19) : tout configuré (logo, adresse, 28 créneaux, 2 catégories) sauf ses prestations → 0 prestation, 0 réservation possible, page publique vide, rendez-vous manqué avec Alexandre. Le TODO pose toujours l'hypothèse que l'écran Prestations bloque le laveur — **voir le tour de table `ideas` ci-dessous : cette hypothèse a de nouveau été vérifiée sur le code actuel, et de nouveau invalidée pour la 3ᵉ journée d'affilée** (déjà fait le 2026-09-10 par `ideas`, revérifié le 2026-09-11 par le manager de l'époque). Le TODO n'a toujours pas été corrigé en conséquence.
  Deux tâches "[PLANIFIÉ DÉBUT SEPTEMBRE 2026]" restent aussi ouvertes (placeholders légaux, passage de Stripe en live) — on est le 12 septembre.
- **Santé du code** : `npm ci` OK · `tsc --noEmit` propre (0 erreur) · `lint` 0 erreur / 23 warnings (motif `set-state-in-effect` assumé, inchangé) · `vitest run` **629/629 tests** verts (46 fichiers, +8 vs hier) · `npm audit --omit=dev` **0 vulnérabilité** (l'audit complet avec les dépendances de dev en trouve 3 modérées, non exposées en prod).
- **Angle mort assumé** : pas d'accès à la base Supabase de production depuis cette routine (aucun secret dans le dépôt) → impossible de dire quoi que ce soit sur l'activité réelle des clients (réservations, trafic, conversions) aujourd'hui.
- **Vérification faite par le manager de cette réunion** (pas un agent), suite au signal de `growth` : `lib/plan.ts` fixe `YEARLY_FREE_MONTHS = 1` (« Engagement annuel : 1 mois offert (on facture 11 mois pour 12) »), avec un commentaire indiquant que l'affichage (`freeMonthsLabel()`) est bien dérivé de cette seule constante — donc le site affiche aujourd'hui "1 mois offert" partout, correctement. Mais `TODO.md` (entrée du 2026-08-26, toujours en place) décrit l'engagement annuel comme **"2 mois offerts (490€/an → 40,83€/mois ; 690€/an → 57,50€/mois)"**. Le calcul documenté (2 mois) ne correspond plus à la constante actuelle du code (1 mois) — soit le TODO est resté périmé après un changement de politique tarifaire non documenté, soit la constante a été modifiée par erreur. À trancher par Alexandre : ce n'est pas un bug visible (le site est cohérent avec lui-même), mais une incohérence entre ce que le TODO annonce comme décision produit et ce qui tourne réellement.

## Tour de table

### seo-geo
Rebranding logo cohérent côté référencement — `alt="WashBoard"` présent partout (landing, dashboard, sidebar), `siteJsonLd.ts` pointe déjà sur le nouveau logo pour le schema `Organization.logo`, icône d'app mise à jour aussi. Rien à corriger. Le fix fuseau horaire et le fix inscription sont hors périmètre SEO.
Ce que je referais : rien à signaler côté contenu ou indexation aujourd'hui — pas de nouvel article, pas de changement de sitemap ou de données structurées à ma main.

### cyber
Le fix d'inscription (`b8f6402`) a été vérifié dans le diff : le filtre qui exclut les aperçus de la vérification anti-doublon de téléphone ne desserre pas la protection sur les vrais comptes (une fiche `NULL` reste comptée comme prise) — pas de régression du type "échec silencieux qui autorise". Le retrait du repli sur l'ancienne variable Maps est cohérent avec la migration Vercel confirmée hier.
Ce que je referais : rien à changer sur mon axe — tsc/lint/tests verts, `npm audit --omit=dev` à 0. Pas d'accès prod aujourd'hui donc pas de vérification RLS/activité réelle possible.

### dev
Le fix fuseau horaire est le genre de bug silencieux qui a pu faire partir des emails/notifications avec une heure de RDV fausse avant le correctif — vaudrait le coup de vérifier s'il y a eu un impact visible côté client plutôt que de considérer le sujet clos une fois le code corrigé. Le retrait du repli Maps est propre, migration confirmée.
Ce que je referais : sur BellAuto89, pousser pour que l'appel d'Alexandre et l'arbitrage produit soient calés cette semaine — chaque jour sans réponse est un essai qui expire (fin le 19/09) sans avoir vu le produit fonctionner. Je signale aussi que les deux tâches "[PLANIFIÉ DÉBUT SEPTEMBRE 2026]" (placeholders légaux, Stripe live) sont en retard sur leur propre échéance affichée dans le TODO.

### ideas
Sur `PrestationsManager.tsx` : le bouton "Enregistrer" (`canSave`, ligne 83) n'exige que nom + prix + durée — catégorie et types de véhicule ne bloquent rien, et depuis le correctif début septembre la première catégorie est même présélectionnée avec tous ses types cochés par défaut. L'hypothèse du TODO ("écran bloqué sans message") ne colle donc plus au code actuel.
Avis tranché sur BellAuto89 : je penche pour l'abandon simple plutôt que le bug d'écran. Reste une inconnue à trancher avant de mobiliser `designer` : son inscription date du 20/08, le correctif du formulaire est plus récent — s'il a tenté de créer sa prestation avant le fix, il a pu buter sur l'ancien comportement (avant, on pouvait enregistrer une prestation sans type coché — confusion différente mais du même ordre). Il suffit de comparer sa dernière activité sur l'écran Prestations à la date du correctif ; cette comparaison demande un accès aux données (Supabase), que cette routine n'a pas.
Ce que je referais : lire le code avant de juger une hypothèse UX au lieu de la prendre pour acquise.

### growth
Le fix inscription (reprise du numéro d'aperçu) est exactement le pont dont l'agent `prospection` a besoin pour convertir des prospects démarchés — bon signal.
Sur BellAuto89 : je vois surtout un point d'onboarding déjà remonté par `ideas`/`dev`. Une relance courte a du sens vu qu'il a déjà pris rendez-vous avec Alexandre (consentement déjà acquis) — angle proposé : "il ne manque qu'une étape pour voir ta page tourner", pas un pitch commercial.
Ce que je referais : j'ai vérifié les chiffres avant de parler prix — `YEARLY_FREE_MONTHS` dans le code vaut 1, pas 2 comme l'affiche encore le TODO (signal remonté au manager, voir Signaux du jour) ; mon argumentaire d'onboarding doit rester calé sur ce que le code fait réellement, pas sur le TODO. Je n'ai pas d'idée d'acquisition à proposer sur l'activité clients réelle : pas d'accès Supabase aujourd'hui, je ne spécule pas.

## Priorités du jour

- **Mettre à jour le TODO sur BellAuto89** : l'hypothèse "écran Prestations bloquant" est invalidée par la lecture du code pour la 3ᵉ journée consécutive sans que le TODO soit corrigé — ça fait revérifier la même chose chaque jour à des agents différents. Prochaine étape utile, qui demande Alexandre : comparer la date de configuration de BellAuto89 à celle du correctif du formulaire, puis relancer avec l'angle "il ne manque qu'une étape" proposé par `growth`. Essai en cours jusqu'au 19/09.
- **Incohérence tarifaire à trancher** : le TODO documente l'engagement annuel comme "2 mois offerts", le code (`YEARLY_FREE_MONTHS`) applique 1 mois. Le site est cohérent avec le code (donc pas de bug visible côté client), mais quelqu'un doit dire laquelle des deux valeurs est la bonne décision produit et corriger l'autre.
- **Deux échéances "début septembre" dépassées** : placeholders légaux et passage de Stripe en live sont toujours ouverts dans le TODO alors qu'on est le 12 — à re-prioriser ou re-dater explicitement.
- Aucun signal de sécurité ou de dette de code nouveau aujourd'hui (typecheck/lint/629 tests/audit tous propres) — rien à traiter dans l'urgence côté technique.
