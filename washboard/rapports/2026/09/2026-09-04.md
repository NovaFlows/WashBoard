# Réunion d'équipe WashBoard — vendredi 4 septembre 2026

## Signaux du jour

**Commits (dernières 26h) :**
- `343fb93` feat(dashboard) : affiche la catégorie du RDV, retire le virement bancaire
- `e82135e` fix(abonnement) : plus de mention de l'annuel dans la vue mensuelle
- `18af99e` docs(signup) : corrige un commentaire inexact sur l'unicité du téléphone
- `ed5b235` feat(offres) : un mois offert, affichage adapté à la périodicité
- `288335f` fix(comptes) : les comptes fantômes se nettoient tout seuls
- `033025b` chore(rapport) : réunion d'équipe du 2026-09-03

**Code :** `tsc --noEmit` propre (0 erreur) · lint 0 erreur / 23 warnings (motif connu `set-state-in-effect`, assumé dans `eslint.config.mjs`) · `vitest run` → **412 tests, tous verts** (31 fichiers) · `npm audit --omit=dev` → **1 vulnérabilité modérée** (package `qs`, bracket-key comma parsing + DoS via `isBuffer`) : provient de `googleapis > googleapis-common > qs`, donc de l'API Google (Maps/Calendar), pas d'un paquet applicatif direct. Déjà signalée hier, toujours pas corrigée.

**TODO.md — 🔴 Priorité haute (items ouverts) :**
- Signal produit BellAuto89 : inscrit le 2026-08-20 (essai jusqu'au 2026-09-19), a tout configuré (logo, adresse, 28 créneaux, 2 catégories) sauf ses prestations → 0 réservation possible. N'est pas venu à un rendez-vous convenu avec Alexandre.
- Action requise d'Alexandre : créer 7 secrets GitHub pour activer le job e2e en CI (sans eux, le job ne teste rien).
- Passage Stripe en live + remplissage des placeholders légaux, prévus début septembre 2026 — la fenêtre est ouverte, rien de nouveau constaté dans les commits de ce champ.
- Item antérieur sur les droits d'écriture GitHub de cette routine cloud (403 constaté le 2026-08-27) : les rapports des jours suivants (dont celui d'hier) ont bien été poussés sur `master` — a priori résolu, à confirmer si le push de ce rapport échoue à nouveau.

⚠️ Aucun accès à la base Supabase de production dans cette session (pas de secrets dans le dépôt) : impossible de dire quoi que ce soit sur l'activité réelle des clients (réservations, paiements, autres essais en cours). Signal manquant, non simulé.

## Tour de table

### seo-geo
Rien à signaler aujourd'hui : aucun commit ne touche au blog, aux balises ou aux données structurées. Point de vigilance indirect : si le blocage prestations de BellAuto89 se reproduit sur la page de réservation publique (la vitrine SEO/GEO du produit), ça dégraderait l'expérience des visiteurs venus par recherche organique ou citation IA. Pas de signal trafic/conversion réel disponible (pas d'accès Supabase). Referait un point dès que les placeholders légaux et le passage Stripe seront traités, pour vérifier l'indexabilité des pages légales.

### cyber
Rien de neuf côté sécurité : les commits du jour (nettoyage comptes fantômes, dashboard, offres) ne touchent pas à l'auth/RLS/secrets à première vue. La vulnérabilité modérée `qs` est à garder à l'œil — pas encore creusée en détail côté chaîne de dépendances, ne pas lancer `audit fix` sans vérifier. Ce qui reste le vrai point mort du jour plus que `qs` : les 7 secrets GitHub toujours manquants, qui privent la CI de tout filet de sécurité automatisé sur le job e2e.

### dev
tsc/lint/vitest tous verts, la série de fixes ciblés (catégorie RDV, comptes fantômes, offres) est propre. Sur BellAuto89 : `ideas` a identifié une cause racine concrète (voir ci-dessous) — à corriger, effort de l'ordre de quelques heures, pas un redesign. La vulnérabilité `qs` reste en dépendance transitive prod (via `googleapis`) : à surveiller, pas de résolution forcée sans vérifier l'impact réel. Pas d'accès prod, donc aucun signal direct sur des bugs vécus par les clients au-delà de ce qui est déjà dans TODO.md.

### ideas
Vérifié dans le code avant de juger le cas BellAuto89, comme hier : `PrestationsManager.tsx` confirme que le bouton "Enregistrer" ne dépend que du nom, du prix et de la durée — catégorie et types de véhicule ne bloquent pas l'enregistrement, contrairement à l'hypothèse écrite dans le TODO. Mais le vrai piège est en aval : une prestation enregistrée **sans aucun type de véhicule coché** devient invisible sur la page de réservation publique (`StepService.tsx` boucle sur `vehicle_types` et n'affiche rien si le tableau est vide) — aucune erreur, aucun message, ni côté client ni côté admin. Ça explique "0 réservation possible" sans que BellAuto89 ait rien vu de cassé. À corriger : validation à la sauvegarde ou a minima un badge "non réservable" dans la liste admin, que `designer` pourra ensuite habiller. Rien d'autre à signaler aujourd'hui.

### growth
BellAuto89 (essai jusqu'au 19/09) reste le signal prioritaire : un onboarding qui bloque juste avant que le client voie le produit fonctionner est le pire endroit pour perdre un essai, et le rendez-vous manqué aggrave le diagnostic. Une fiche prospect avec accroche existe déjà — proposition : relancer aujourd'hui avec un message concret ("il te reste juste une étape de 2 minutes pour voir tes clients réserver") plutôt qu'un rappel générique de RDV manqué. Sans accès Supabase, impossible de savoir si d'autres essais butent au même endroit — à vérifier avant de traiter BellAuto89 comme un cas isolé.

## Priorités du jour

- **BellAuto89** : cause racine identifiée par `ideas` (prestation sans type de véhicule coché = invisible et non réservable côté public, sans aucun message d'erreur nulle part) — corriger côté `dev` (validation à la sauvegarde ou badge "non réservable" dans l'admin), puis relancer BellAuto89 avant l'expiration de l'essai le 2026-09-19.
- **7 secrets GitHub** toujours à créer par Alexandre pour que le job e2e de la CI teste réellement quelque chose.
- **Vulnérabilité `qs`** (modérée, via `googleapis`) : signalée depuis deux jours, toujours pas traitée — à vérifier avant `npm audit fix`.
- **Stripe live + placeholders légaux** : fenêtre "début septembre" ouverte, aucun mouvement constaté dans les commits — à ne pas laisser glisser.
