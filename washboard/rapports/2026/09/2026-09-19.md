# Réunion d'équipe WashBoard — samedi 19 septembre 2026

## Signaux du jour

- **Commits (26h) :** aucun commit de code. Le seul commit récent est celui du rapport de réunion d'hier (`875acf3`).
- **Priorité haute (TODO.md) :** un seul item ouvert — dépassement probable de quota Supabase (bande passante egress). Base 14 Mo / fichiers 2,1 Mo, loin des limites de stockage : la cause probable est que `getBgStyle` sert les fonds/logos directement depuis l'URL publique Supabase (`background-image`), sans passer par `/_next/image` ni cache Vercel, à chaque visite d'une page de réservation. Amplifié par une vidéo TikTok virale du 2026-09-04. **Deadline 5 octobre 2026** avant restriction du projet Supabase (couperait les pages de réservation de tous les laveurs).
- **Santé du code :** `npx tsc --noEmit` → 0 erreur. `npm run lint` → 0 erreur, 26 warnings (tous le même motif `set-state-in-effect`, déjà assumé dans `eslint.config.mjs`). `npx vitest run` → **856 tests passent** sur 72 fichiers, 0 échec. `npm audit --omit=dev` → **0 vulnérabilité** sur les dépendances de production (3 modérées en dev-only, vues lors de `npm ci`, non auditées ici).
- **Hors de portée aujourd'hui :** pas d'accès à la base Supabase de production — aucun signal sur l'activité réelle des clients (réservations, paiements, egress mesuré en direct).

## Tour de table

### seo-geo
Rien de neuf côté SEO/GEO depuis hier, aucun commit sur le contenu ou les balises à examiner. Si les fonds/logos coupent ou dégradent l'affichage des pages publiques à cause de l'egress Supabase, c'est un signal négatif pour Google (Core Web Vitals, images cassées) et pour les moteurs IA qui crawlent ces pages — pas le lead sur la résolution, mais veut être prévenu avant toute coupure pour vérifier l'impact sur les balises/JSON-LD. Ce que je referais : continuer à mesurer avant de proposer, pas de nouvelle recommandation inventée faute de données de trafic réelles.

### cyber
Rien de neuf côté sécurité — le sujet egress Supabase n'est pas un sujet permission/RLS, donc hors radar habituel ; l'audit prod à 0 vuln confirme que l'état du 27/08 était bien du résiduel dev-only. Zéro visibilité sur les logs Supabase réels, donc aucune activité suspecte n'est signalée. Ce que je referais : quand le fix egress sera codé (cache/CDN pour les fonds/logos), vérifier que les URLs Supabase Storage restent en lecture publique voulue — signalé à `dev` au moment du diff plutôt que maintenant.

### dev
Rien de neuf côté code aujourd'hui : pas de commit en 26h, tsc/lint/vitest/audit tous verts. Le sujet du jour reste le quota Supabase — la piste `getBgStyle` (URL publique en `background-image` direct, sans `/next/image`) est cohérente avec un pic egress suite au TikTok viral, mais pas d'accès à la page Usage Supabase pour confirmer, donc rien n'est tranché avant cette vérification. Ce que je referais : dès que les chiffres egress réels sont disponibles, préparer la migration de `getBgStyle` vers l'optimisation d'image Next — mais rien n'est implémenté tant que la cause n'est pas confirmée, pour éviter de corriger le mauvais problème.

### ideas
Rien à trancher côté produit — 26h sans commit, code sain, pas d'accès prod pour évaluer un signal client réel. Le seul point chaud (egress Supabase) est un sujet infra/coût pour `dev`, pas un jugement fonctionnel. Ce que je referais : ne pas inventer une feature pour occuper ce point — vu le pitch (« tu laves des voitures, on gère le reste »), forcer une idée sans signal irait à l'encontre de la discipline attendue.

### growth
Rien de neuf côté marketing/commercial — pas de nouvelle donnée d'usage, et l'enjeu du jour (quota Supabase) est technique, hors périmètre direct. Inquiétude : si les pages de réservation sautent début octobre, c'est l'outil de démo commerciale et la page publique de Kooki Clean qui tombent — un risque indirect mais réel. Ce que je referais : continuer à ne rien avancer côté acquisition/argumentaire tant qu'il n'y a pas de vraies données d'usage — pas de campagne ni de nouvelle promesse commerciale sans savoir si le client bêta réserve encore.

### legal
Rien de neuf côté légal — aucune nouvelle donnée personnelle collectée, pas de texte à publier. Point indirect : si le quota Supabase saute le 5 octobre et coupe les pages de réservation publiques, une coupure prolongée pourrait toucher l'engagement de disponibilité implicite envers les laveurs abonnés (à vérifier dans les CGV le moment venu, pas urgent aujourd'hui). Ce que je referais : rien à changer — pas de texte légal touché tant que l'entité n'existe pas et qu'Alexandre n'a rien confirmé.

### designer
Rien de neuf côté design aujourd'hui — pas de commit, pas de nouvelle page à auditer. Le fix egress proposé (`background-image` → `/_next/image`) est aussi une bonne direction visuelle : formats modernes (WebP/AVIF) et résolution adaptée à l'écran, en général meilleure netteté mobile qu'un fond CSS brut — à condition de bien fixer `object-fit: cover` et les breakpoints `sizes` pour ne pas perdre le cadrage actuel. Pas de donnée sur le rendu réel en prod, donc pas d'avis définitif sans capture avant/après une fois migré. Ce que je referais : rien à changer dans ma façon de faire, pas de sujet visuel ouvert aujourd'hui.

### prospection
Rien à signaler aujourd'hui — aucun numéro ni prospect confié récemment, pas de fichier prospects présent sur le disque.

### video
Rien à signaler aujourd'hui, aucun rendu ni voix-off en cours. À noter : la vidéo TikTok du 2026-09-04 a été un vrai succès viral — ça vaut le coup d'en discuter avec `growth` pour une déclinaison si Alexandre veut capitaliser dessus.

## Priorités du jour

- **Quota Supabase (deadline 5 octobre) :** confirmer la cause exacte (page Usage de l'organisation Supabase) avant de coder quoi que ce soit — `dev` est prêt à migrer `getBgStyle` vers `/_next/image` dès confirmation, avec un passage `designer` sur le rendu visuel (formats, cadrage) et `cyber` à vérifier sur les permissions du bucket au moment du diff.
- **Continuité de service à surveiller :** `growth` et `legal` signalent tous deux qu'une coupure des pages de réservation début octobre toucherait à la fois la démo commerciale (Kooki Clean) et, si prolongée, l'engagement de disponibilité envers les laveurs abonnés — à garder en tête sans action immédiate.
- **Capitaliser sur le succès TikTok du 04/09 :** `video` et `growth` proposent d'envisager une déclinaison si Alexandre veut relancer l'effet viral — décision à prendre par Alexandre, rien d'engagé.
- **Rien à signaler de plus :** code sain (tsc/lint/tests/audit tous verts), aucune activité prospection/vidéo confiée récemment, pas de nouveau sujet produit/légal/SEO à trancher aujourd'hui.
