# Réunion d'équipe WashBoard — dimanche 6 septembre 2026

## Signaux du jour

- **Commits (26 dernières heures) : 14**, quasi tous des `fix`. Grosse série d'audit sécurité
  (`fix(securite)`, 4× `fix(audit)` : grâce 30 jours, cloisonnement des routes `[id]`, CSRF sur la
  connexion Google, échappement HTML + SSRF, idempotence des webhooks Stripe), deux correctifs
  images/quota Supabase, un fix mobile (dashboard zoomé après connexion iPhone), une confirmation
  avant changement de lien de réservation (+ fix du contrôle d'unicité du lien), un plafond de coût
  sur les cinq routes publiques Google Maps, et un correctif d'attribution sur la page de
  réservation publique (elle annonce le nom du laveur, plus WashBoard).
- **Santé du code** : `npm ci` propre (677 paquets) · `tsc --noEmit` : 0 erreur · `lint` : 0 erreur,
  23 warnings (baseline connue, motif `set-state-in-effect` assumé + `<img>` non optimisées) ·
  `vitest run` : **594/594 tests verts** (43 fichiers, en hausse par rapport aux 371 du 30/08) ·
  `npm audit --omit=dev` : **0 vulnérabilité**.
- **TODO.md — 🔴 Priorité haute** : le signal produit BellAuto89 (inscrit ayant tout configuré sauf
  ses prestations, rendez-vous manqué avec Alexandre) reste ouvert, ainsi que la demande d'action
  à Alexandre pour créer 7 secrets GitHub afin que le job e2e de la CI tourne réellement.
- **Hors périmètre de cette routine** : aucun accès à la base Supabase de production — impossible
  de dire quoi que ce soit sur l'activité réelle des clients (réservations, paiements, nouveaux
  essais).

## Tour de table

### seo-geo
Le fix `fix(partage)` de la nuit correspond exactement à un point déjà soulevé en réunion
précédente (canonical hérité qui pointait toutes les pages `/book/*` vers l'accueil, OG héritant
du pitch B2B) : c'est corrigé proprement (noindex/follow, canonical propre par laveur, OG avec son
nom/logo) — bonne décision, ces pages sont transactionnelles et n'ont rien à faire indexées. Le
plafond de coût Google Maps ne le concerne pas (aucune balise/contenu touché). Rien à changer ;
vérification prévue au prochain build que le sitemap n'inclut toujours pas ces routes `/book/*`.

### cyber
Les quatre `fix(audit)`/`fix(securite)` de la nuit couvrent exactement le motif surveillé (échec
silencieux → comportement permissif) : grâce RGPD réelle, cloisonnement des routes `[id]`, CSRF
Google, SSRF/échappement HTML, idempotence webhook Stripe — bon signe. Le `fix(couts)` plafonne
les cinq routes Maps avec un compteur partagé, bon réflexe budgétaire. Reste à vérifier
personnellement (pas juste sur la foi du message de commit) que ces correctifs loggent bien
l'échec et pas seulement qu'ils bloquent. `npm audit` à 0 vulnérabilité confirmé, en progrès par
rapport aux 9 du 27/08.

### dev
Rien d'alarmant en suspens : tsc/lint/594 tests/audit tous verts après la série de correctifs
sécurité. Deux petits bugs UX corrigés vite (lien de réservation, zoom mobile iPhone) montrent
qu'on découvre encore des régressions en prod plutôt qu'en test. La dette des composants
monolithiques (~80h, CalendrierDashboard et consorts) reste intacte et s'alourdit un peu à chaque
fix qui y touche — pas d'action sans feu vert d'Alexandre, mais un vrai créneau dédié vaudrait
mieux que d'empiler.

### ideas
Vérification faite sur le signal BellAuto89 : le commit `380796a` du 4 septembre
("les types sont cochés d'emblée" dans `PrestationsManager.tsx`) corrige exactement le blocage
décrit — avant, changer de catégorie ne cochait aucun type sans indiquer que c'était nécessaire
pour enregistrer. Le compte s'est bloqué entre le 20 et le 31 août, donc **avant** ce correctif :
cohérence temporelle confirmée, et le code actuel (`canSave`) ne dépend plus de la
catégorie/des types. Conclusion : pas la peine de refaire juger l'écran par `designer`/`ideas`,
il est déjà corrigé. Le point utile pour l'appel : BellAuto89 doit rouvrir l'écran Prestations
pour bénéficier du fix — le code à jour ne suffit pas si son compte n'a pas rechargé la page.

### growth
BellAuto89 a fait tout le fastidieux (logo, adresse, 28 créneaux, catégories) sauf les
prestations — sans elles, sa page publique ne peut rien proposer, et il n'a jamais vu le produit
tourner ; il a aussi loupé un rendez-vous avec Alexandre. Signal d'onboarding, pas manque
d'intérêt : l'appel doit porter sur "on finit ensemble les 5 minutes qui manquent" (et maintenant,
sur le fait qu'un correctif est passé depuis), pas relancer le rendez-vous manqué comme sujet en
soi. Le fix `fix(partage)` (le laveur affiché, pas WashBoard) va dans le bon sens du
positionnement — l'outil reste invisible au profit du métier du laveur. Rien d'autre de neuf côté
marketing/commercial aujourd'hui.

## Priorités du jour

- **Appeler BellAuto89** avec une accroche à jour : le blocage qu'il a rencontré sur l'écran
  Prestations est corrigé depuis le 4 septembre (`ideas` l'a vérifié dans le code), mais son
  compte doit rouvrir la page pour en profiter — ce n'est plus un sujet produit ouvert, c'est un
  appel commercial à faire.
- **Action toujours en attente d'Alexandre** : créer les 7 secrets GitHub listés dans TODO.md
  pour que le job e2e de la CI tourne réellement (aujourd'hui il s'arrête proprement mais ne
  teste rien).
- `cyber` prévoit de vérifier par lui-même (pas seulement sur la foi des messages de commit) que
  les correctifs de sécurité de la nuit loggent bien leurs échecs.
- `dev` signale que la dette des composants monolithiques (~80h) continue de s'alourdir au fil
  des correctifs qui la traversent — à arbitrer avec Alexandre pour un créneau dédié, sans
  urgence.
