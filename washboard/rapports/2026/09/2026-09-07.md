# Réunion d'équipe WashBoard — lundi 7 septembre 2026

## Signaux du jour

**Commits (dernières 26h, 6 au total)** : `fix(app)` bandeau d'annonce éclairci, `feat(app)` annonce
de l'application mobile en bêta (+ comment l'avoir), `fix(inscription)` un numéro exempté ne
pouvait plus créer de compte du tout, `feat(push)` notification sur le téléphone d'Alexandre à
chaque nouvelle inscription, `feat(landing)` élargissement du positionnement au-delà du lavage
auto, `fix(observabilité)` traçage des 32 lectures Supabase muettes des routes API et cron. Le
dernier commit de la veille était le rapport du 2026-09-06.

**Santé du code** : `npm ci` propre, `tsc --noEmit` sans erreur, `npm run lint` → 23 avertissements
/ 0 erreur (baseline assumée `set-state-in-effect`, inchangée), `vitest run` → **616/616 tests
verts** (45 fichiers), `npm audit --omit=dev` → **0 vulnérabilité**.

**TODO.md — Priorité haute** : le signal produit BellAuto89 (inscrit avec 0 prestation configurée,
RDV manqué, essai jusqu'au 19/09) reste ouvert dans le fichier ; l'action « créer les 7 secrets
GitHub pour la CI e2e » attend toujours Alexandre ; les placeholders légaux et le passage de
Stripe en live restent planifiés pour début septembre.

**Point de vigilance vérifié** : le fichier note depuis le 2026-08-27 que la routine cloud n'avait
pas les droits d'écriture GitHub (403 au push). Vérification faite ce matin : `origin/master` est
synchronisé avec le dernier commit local après un `git fetch` frais — le push de ce rapport
confirmera si le problème est réellement résolu ou seulement intermittent.

**Angle mort assumé** : aucun accès à la base Supabase de production dans cette session — pas de
donnée réelle sur les inscriptions, réservations ou paiements du jour. Ce qui suit ne s'appuie
que sur le code, les commits et le TODO.

## Tour de table

### seo-geo
Le commit `feat(landing)` a réécrit le H1, le pitch et le footer visibles pour parler de
« nettoyage et entretien à domicile », mais sans toucher aux balises : `<title>`, meta description,
OG tags (`layout.tsx`) et le JSON-LD (`siteJsonLd.ts`) parlent encore exclusivement de « laveurs
auto mobiles ». Le contenu visible et les données structurées racontent deux positionnements
différents à Google et aux IA, ce qui peut brouiller le rich snippet et une réponse GEO. Je
referais un changement de positionnement en alignant balises et JSON-LD dans la même PR — et je
vais clarifier avec `growth` si cet élargissement est définitif ou un test avant de retoucher quoi
que ce soit.

### cyber
Revu les deux commits qui me concernent. Le fix inscription suit le bon pattern : la vérification
du numéro exempté est faite avant la requête, et l'échec de lecture reste tracé et bloque (503)
plutôt que de basculer en « laisser passer » — pas de régression du motif qu'on traque cette
semaine. La notification push ne fuit rien (liste d'admins via variable d'env, pas de secret dans
le payload) et un échec d'envoi ne bloque jamais l'inscription. Rien à corriger sur ces deux
points ; je vérifierai juste, sans urgence, que la liste des numéros exemptés reste courte et à
jour.

### dev
Le fix inscription touchait à l'auth — bon réflexe de l'avoir corrigé vite, mais ça mérite un test
de non-régression s'il n'existe pas déjà. Les 32 lectures muettes tracées, c'est du bon travail ;
il en reste 23, dont `travelFee.ts` qui a déjà un historique de repli silencieux à 0 € — celle-là
ne devrait pas traîner. Je referais : prioriser `travelFee.ts` et les 4 autres lectures de `lib/`
avant les 18 des pages dashboard (impact silencieux plus grave côté argent que côté affichage), et
laisser BellAuto89 à `designer`/`ideas` plutôt que de trancher le message d'erreur moi-même.

### ideas
BellAuto89 reste à 0 prestation, RDV manqué, essai expire dans 12 jours — et c'est déjà le sujet du
4/09. Revérifié `PrestationsManager.tsx` : le bouton Enregistrer ne dépend que de nom/prix/durée,
catégorie et types sont pré-cochés depuis le fix du 4/09 (`380796a`) — l'hypothèse « écran qui
coince » ne tient plus techniquement, aucun commit depuis n'a retouché ce fichier ni
`StepService.tsx`. Ça ressemble à un décrochage réel, pas à un bug qui bloque. Je referais :
arrêter de revalider la même hypothèse chaque jour sans agir — relancer BellAuto89 directement
avant le 19/09 plutôt que d'espérer un nouveau signal produit d'un compte qui n'est pas revenu
depuis le RDV manqué.

### growth
Le cas BellAuto89 est plus commercial que produit : un RDV manqué avec zéro prestation configurée,
c'est un prospect qui a buté sur l'onboarding, pas un mauvais fit. L'élargissement du positionnement
landing m'inquiète : le pitch actuel est spécifique et cohérent avec la cible artisan solo, et je
n'ai pas vu de demande client documentée qui justifie de le diluer — ça touche direct à l'accroche
que je fournis à `prospection`. Je referais : relancer BellAuto89 avec l'accroche déjà en fiche, en
positionnant l'appel comme « on configure les prestations ensemble en 5 min » plutôt que comme un
simple rappel ; et je veux voir le contenu réel du changement de positionnement avant tout avis
définitif, en consultant `seo-geo` en direct.

## Priorités du jour

- **Positionnement landing élargi** : signal contradictoire entre `seo-geo` (balises et JSON-LD
  encore mono-thème « lavage auto », désynchronisées du contenu visible déjà élargi) et `growth`
  (aucune demande client documentée ne justifie cet élargissement, risque de diluer l'accroche
  commerciale) — à trancher avec Alexandre avant d'aller plus loin.
- **BellAuto89** : `dev` et `ideas` convergent — la piste technique ne tient plus depuis le fix du
  4 septembre, ce n'est plus un sujet produit ouvert. Appeler maintenant (essai jusqu'au 19/09),
  avec l'angle « on finit les prestations ensemble » plutôt qu'un simple rappel de RDV manqué.
- **Lectures Supabase encore muettes** : `dev` recommande de traiter en priorité les 5 restantes de
  `lib/` (dont `travelFee.ts`, qui a déjà causé un repli silencieux à 0 €) avant les 18 des pages
  dashboard.
- **Toujours en attente d'Alexandre** : créer les 7 secrets GitHub listés dans `TODO.md` pour que
  le job e2e de la CI tourne réellement.
