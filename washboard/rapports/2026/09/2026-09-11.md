# Réunion d'équipe WashBoard — vendredi 11 septembre 2026

## Signaux du jour

- **3 commits** dans les dernières 26h, tous sur la feature interne « prospects » (génération de visuels de prospection) :
  - `feat(prospects)` — le mur d'atelier devient le fond par défaut.
  - `fix(prospects)` — un logo en bandeau est désormais complété en carré avant l'envoi.
  - `fix(prospects)` — la resynchronisation ne rend plus la couleur illisible.
  - Aucun de ces commits ne touche le produit washboard vu par les laveurs/clients.
- **Section 🔴 Priorité haute du TODO** — 2 points encore ouverts :
  1. **BellAuto89** (inscrit le 2026-08-20, essai jusqu'au 2026-09-19) : a tout configuré (logo, adresse, 28 créneaux, 2 catégories) sauf ses prestations → 0 prestation, 0 réservation possible, page publique vide. N'est pas venu à un rendez-vous convenu avec Alexandre. Le TODO pose toujours l'hypothèse que l'écran Prestations bloque le laveur.
  2. **Action requise d'Alexandre** : créer les 7 secrets GitHub (`E2E_SUPABASE_URL`, `E2E_SUPABASE_ANON_KEY`, `E2E_SUPABASE_SERVICE_ROLE_KEY`, `E2E_WASHER_EMAIL`, `E2E_WASHER_PASSWORD`, `E2E_WASHER_SLUG`, `E2E_CLIENT_EMAIL`) pour que le job e2e de la CI tourne réellement — aujourd'hui il s'arrête proprement sans rien tester.
- **Santé du code** : `npm ci` OK · `tsc --noEmit` propre (0 erreur) · `lint` 0 erreur / 23 warnings (motif `set-state-in-effect` assumé, inchangé) · `vitest run` **621/621 tests** verts (45 fichiers) · `npm audit --omit=dev` **0 vulnérabilité** (l'audit complet avec les dépendances de dev en trouve 3 modérées, non exposées en prod).
- **Angle mort assumé** : pas d'accès à la base Supabase de production depuis cette routine (aucun secret dans le dépôt) → impossible de dire quoi que ce soit sur l'activité réelle des clients (réservations, trafic, conversions) aujourd'hui.
- **Vérification faite par le manager de cette réunion** (pas un agent) : relecture directe de `PrestationsManager.tsx`. Le bouton « Enregistrer » (`canSave`, ligne 83) ne dépend que de `name`, `price` et `duration_minutes` — aucune condition sur la catégorie ou les types cochés. Le bouton cité par `dev` (lignes 291-292, « + Ajouter ») est en fait celui d'un supplément (label + prix), pas un contrôle de type. **Ce constat confirme celui déjà posé par `ideas` hier (rapport du 2026-09-10)** — et le TODO n'a toujours pas été corrigé en conséquence : l'hypothèse « écran Prestations bloquant » y est reformulée à l'identique aujourd'hui, ce qui a fait revérifier deux fois la même chose à deux agents (`dev` et `ideas`) à un jour d'intervalle.

## Tour de table

### seo-geo
Rien à signaler côté SEO/GEO aujourd'hui : les 3 commits touchent uniquement l'interne « prospects », aucun impact sur le site public ni le blog. Le cluster de 4 articles et le sitemap sont en place, mais pas d'accès à la Search Console depuis cette routine — donc pas de nouveau signal de trafic à remonter, à regarder dans une semaine comme prévu au TODO.
Ce que je referais : ne pas attendre passivement — préparer dès maintenant un brouillon de 5ᵉ article pour tenir le rythme de publication, et relancer Alexandre pour valider les fourchettes de prix de l'article tarifs avec son expérience terrain plutôt que de laisser cette action traîner sans échéance.

### cyber
Rien à signaler côté sécurité aujourd'hui : les 3 commits sont confirmés cantonnés à la feature « prospects » (habillage visuel, logo, couleurs) — aucun fichier touchant auth, routes API ou RLS. `npm audit` prod à 0, tsc et lint propres (les 3 vulnérabilités modérées restent dev-only, sans impact prod).
Un point reste bloquant pour Alexandre, pas pour moi : les 7 secrets GitHub Actions à créer pour que le job e2e tourne réellement — en attendant, la CI passe au vert sans rien tester sur ce chemin, donc prudence si on s'y fie pour valider une release. Pas d'accès à la base Supabase de prod depuis ici : signal manquant, pas un feu vert.

### dev
Sur `PrestationsManager.tsx` : le bouton « Enregistrer » est `disabled={!canSave}` sans texte associé expliquant la condition manquante — le signal terrain sur BellAuto89 semblait coller avec ce que je voyais dans le code, à faire trancher par `designer`/`ideas`.
*(Note du manager : cette lecture est incomplète — voir la vérification directe plus haut, qui montre que `canSave` ne dépend pas de la catégorie/des types. Le sujet a par ailleurs déjà été tranché par `ideas` dans le rapport d'hier.)*
Reste propre : tsc/lint/621 tests verts, 0 vuln prod, les 3 commits d'hier restent cantonnés à la feature prospects sans toucher au reste. Pas touché à la dette `CalendrierDashboard` tant qu'Alexandre n'a pas tranché, comme convenu.

### ideas
La piste « écran Prestations bloqué » a déjà été creusée : le commit `380796a` (4 septembre) confirme que le bouton Enregistrer n'a jamais exigé de type coché, seulement nom/prix/durée — donc pas de blocage technique dur à cet endroit, contrairement à l'hypothèse du TODO. Reste une friction réelle *avant* ce correctif (il fallait cocher chaque type à la main sans indication), donc la question qui compte est : la configuration de BellAuto89 (28 créneaux, 2 catégories, inscrit le 20/08) a-t-elle eu lieu avant ou après le 4/09 ?
Sans accès à Supabase, ce signal manque — je ne l'invente pas. Ce que je referais : avant de rouvrir un ticket produit sur cet écran, demander à `dev` de croiser l'horodatage des créneaux/catégories de BellAuto89 avec le déploiement du commit `380796a` plutôt que de repartir sur une hypothèse déjà en partie invalidée.

### growth
Ce que je remarque : BellAuto89 a fait tout le travail fastidieux (logo, adresse, 28 créneaux, 2 catégories) mais zéro prestation créée — il n'a jamais vu le produit tourner en vrai, et il a manqué le rendez-vous avec Alexandre. C'est un signal d'onboarding, pas de désintérêt : personne ne configure 28 créneaux pour abandonner ensuite.
Ce que je referais : l'appel doit mener avec « je t'aide à créer ta première prestation en 2 minutes en direct » plutôt qu'un discours commercial — l'accroche de la fiche prospect va dans ce sens, à garder. Pas de donnée d'usage réelle (essais en cours, taux de blocage à cette étape) pour dire si c'est un cas isolé ou un pattern du funnel — à vérifier avec Alexandre avant toute conclusion générale sur l'onboarding.

## Priorités du jour

- **TODO à mettre à jour sur BellAuto89** : l'hypothèse « écran Prestations bloquant » ne résiste toujours pas à la lecture du code (confirmé deux jours de suite, par `ideas` hier et par vérification directe aujourd'hui) — mais le TODO garde la formulation d'origine, ce qui fait revérifier la même chose chaque jour. Prochaine étape utile : croiser la date de configuration de BellAuto89 avec le 4 septembre (correctif des types pré-cochés) pour savoir si son blocage est antérieur ou postérieur au fix.
- **Appel BellAuto89** : angle de relance suggéré par `growth` — déblocage en direct (« 2 minutes pour créer ta première prestation »), pas argumentaire commercial.
- **7 secrets GitHub pour la CI e2e** : toujours en attente côté Alexandre — sans eux, aucune régression auth/API n'est couverte par les tests e2e, `cyber` le rappelle.
- Aucun signal de dette de code nouveau aujourd'hui (typecheck/lint/tests/audit tous propres) — rien à traiter dans l'urgence côté technique.
