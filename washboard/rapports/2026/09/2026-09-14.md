# Réunion d'équipe WashBoard — lundi 14 septembre 2026

## Signaux du jour

- **14 commits** dans les dernières 26h : contenu landing ("produit en vrai", fonctionnalités, pour qui, FAQ), nav pleine largeur + liens de section surlignés, correctifs de nav (bug sur écran de faible hauteur), correctif dashboard (logo 40px), favicon régénéré (16/32/48px), correctif positionnement (dernières mentions "voiture propre" liées aux avis Google), nouvel agent `video` (Remotion) + leçons du premier montage TikTok, `.env.example` réaligné sur les variables réellement lues.
- **TODO — Priorité haute** : un seul point ouvert. L'inscrit **BellAuto89** (essai en cours jusqu'au 2026-09-19) a configuré logo, adresse, 28 créneaux et 2 catégories, mais **aucune prestation** → 0 réservation possible sur sa page publique. Il avait un rendez-vous avec Alexandre et ne s'est pas présenté. Fiche prospect déjà prête pour le rappeler.
- **Santé code** : `tsc --noEmit` clean (0 erreur), `npm run lint` 0 erreur / 23 warnings (tous `react-hooks/set-state-in-effect` sur le pattern de montage client + 1 import inutilisé), `vitest run` : **658 tests passent** sur 49 fichiers, `npm audit --omit=dev` : **0 vulnérabilité** en production (3 modérées, mais uniquement en devDependencies).
- ⚠️ **Signal manquant** : pas d'accès à la base Supabase de production depuis cette routine — aucune donnée sur l'activité réelle des laveurs/clients (inscriptions, réservations, paiements, logs).

## Tour de table

### seo-geo
Rien dans les commits du jour ne touche au SEO/GEO (landing, nav, favicon, dashboard) — pas de risque de régression sur les balises ou données structurées. Si l'écran Prestations bloque vraiment en prod, il vaudra la peine de vérifier que la page de réservation publique reste correctement indexable/citable même sans aucune prestation configurée (état vide affiché aux crawlers/IA). Rien à corriger aujourd'hui ; vérification rapide prévue que le lot landing/favicon n'a pas touché aux `PLAN_CARDS` ni au JSON-LD sans le signaler.

### cyber
Le `.env.example` réaligné sur les variables réellement lues est un bon signal — à vérifier à l'occasion qu'aucune clé serveur n'y est mal préfixée `NEXT_PUBLIC_`. Les 3 vulnérabilités modérées de `npm audit` sont en devDependencies uniquement, donc hors surface d'attaque prod : rien d'urgent. Aucun changement de route API ni de RLS/GRANT dans ces commits. Sans accès aux logs Supabase de prod, impossible de confirmer l'absence de tentative d'abus sur la page de réservation publique (honeypot/rate-limit) — accès toujours à obtenir. Le blocage BellAuto89 n'est pas un sujet sécurité.

### dev
Aucun des 14 commits ne touche au flux Prestations ou à la validation de formulaire — le blocage de BellAuto89 n'est probablement pas une régression récente. Code santé : tsc/lint/658 tests clean, rien à signaler côté technique. Ne touchera pas l'écran Prestations sans cadrage préalable par `ideas` puis `designer`.

### ideas
A lu le code de `PrestationsManager.tsx` avant de juger : l'hypothèse du TODO ("bouton verrouillé sans catégorie + type coché") **ne correspond pas au code actuel** — `canSave` ne dépend que de nom/prix/durée. Le vrai piège identifié est ailleurs : une prestation n'existe qu'après avoir créé une catégorie au préalable (deux écrans à enchaîner, lien implicite, simple avertissement discret), et choisir "— Sans catégorie —" **vide silencieusement les types déjà cochés** sans avertir que ça rend la prestation invendable — ce qui correspond exactement au symptôme "0 réservation possible". Priorité : l'appel à BellAuto89 pour trancher abandon vs blocage réel ; si blocage confirmé, orienter `designer` sur l'étape catégorie→prestation (état vide plus explicite, garde-fou si 0 type sélectionné) plutôt que sur `canSave`.

### growth
La landing s'étoffe bien (fonctionnalités/pour qui/FAQ, positionnement corrigé suite aux avis Google) — cohérent avec l'usage de la page publique comme outil de démo commercial. Sur BellAuto89 : essai jusqu'au 19/09, page vide, rendez-vous manqué — un client en train de décrocher, pas seulement un blocage technique. À relancer aujourd'hui avec l'accroche déjà en fiche prospect, ton direct sans reproche, avant l'expiration de l'essai dans 5 jours. Le diagnostic technique reste à `designer`/`ideas`.

## Priorités du jour

- **Appeler BellAuto89 aujourd'hui** (growth) — l'essai expire dans 5 jours et la page publique est vide ; accroche déjà prête en fiche prospect.
- **Le vrai bug potentiel n'est pas celui décrit dans le TODO** (ideas) : ce n'est pas le bouton "enregistrer" qui se verrouille, mais l'enchaînement catégorie→prestation et le fait que choisir "— Sans catégorie —" efface silencieusement les types cochés. À confirmer avec BellAuto89 puis transmettre à `designer` pour corriger cet écran précis, pas `canSave`.
- **Obtenir un accès en lecture aux logs Supabase de prod pour `cyber`** — actuellement aucune visibilité sur d'éventuels abus de la page de réservation publique.
- Rien d'urgent côté code/sécurité : tests, typecheck et lint sont propres, 0 vulnérabilité en production.
