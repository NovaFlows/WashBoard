# Réunion d'équipe WashBoard — mercredi 9 septembre 2026

## Signaux du jour

- **Commits (26 dernières heures) :** 9 commits, tous concentrés sur un nouveau chantier
  « prospects » — fabrication d'une page de réservation de démo, aux couleurs du prospect
  (logo incrusté, fond généré), **avant même son inscription**. Outil de prospection
  commerciale, distinct des comptes clients réels.
- **TODO.md — priorité haute :** 2 items ouverts :
  1. Signal produit sur **BellAuto89** (essai en cours, tout configuré sauf les
     prestations → 0 réservation possible, rendez-vous manqué avec Alexandre).
  2. Action requise d'Alexandre : créer 7 secrets GitHub pour que le job e2e de la CI
     tourne réellement (il s'arrête proprement sans eux, mais ne teste rien).
- **Santé du code :** `tsc --noEmit` 0 erreur · lint 0 erreur (23 warnings connus/acceptés,
  motif `set-state-in-effect`) · `vitest run` **619/619 tests verts** (45 fichiers) ·
  `npm audit --omit=dev` **0 vulnérabilité** (`npm ci` signale 3 vulnérabilités modérées
  au total, mais uniquement dans des dépendances de dev).
- **Angle mort assumé :** pas d'accès à la base Supabase de production depuis cette
  routine — aucun signal sur l'activité réelle des clients (réservations, paiements,
  trafic). Ce signal manque, il n'est pas simulé.

## Tour de table

### seo-geo
Rien de nouveau sur mon périmètre (contenu, balises, sitemap, JSON-LD). Vérifié que
`/book/[slug]` reste en `noindex`, y compris pour les pages « proposition » du chantier
prospects — pas de risque d'indexation de pages factices. Point indirect à noter : si
BellAuto89 reste à 0 prestation, sa page publique **réelle**, elle, est indexable et vide
pour Google comme pour un visiteur — un sujet produit, pas SEO, mais qui mérite d'être
signalé à `dev` si ce n'est pas déjà fait.

### cyber
Vérifié le nouveau code prospects : le flag `is_preview` est bien refusé **côté serveur**
dans `api/bookings/route.ts` (pas seulement dans l'UI), donc pas de faille de réservation
fantôme sur les pages de démo. Les scripts `prospects/*.mjs` tournent en local avec la
clé `service_role` sur la base de prod — pas de nouvelle surface exposée côté app pour
l'instant. Rien d'autre à signaler aujourd'hui ; point à surveiller si ces scripts
gagnent un jour un mode d'écriture batch/CI.

### dev
Confirmé le même point que `cyber` : le rejet des pages de démo est fait côté route API,
avec un log dédié (`bookings.rejected.page_proposition`) — le bon réflexe après le
honeypot de la semaine. Rien d'autre de neuf côté dev. Le point réellement bloquant du
jour reste BellAuto89 ; j'avais proposé de vérifier si l'écran « Prestations » bloque
silencieusement (bouton désactivé sans message) — voir la vérification de `ideas`
ci-dessous, qui referme cette piste.

### ideas
Vérifié le code de `PrestationsManager.tsx` : la condition qui active le bouton
d'enregistrement (`canSave`) ne dépend **ni** de la catégorie **ni** des types de
véhicule — l'hypothèse du TODO (« l'écran Prestations coince ») **ne tient pas** sur le
code actuel. BellAuto89 a donc probablement juste été interrompu, pas bloqué par un bug
d'UX généralisé à tous les inscrits. Je recommande de corriger l'item TODO en ce sens
plutôt que de le laisser en l'état. Par ailleurs, le chantier « prospects » stocke logo
et couleurs d'un tiers qui n'est pas encore client, avant tout consentement explicite —
je le signale pour que `legal` tranche, je ne le fais pas moi-même.

### growth
Le chantier « prospects » est le bon outil pour appuyer le pitch « montrer plutôt que
lister » — utile pour la démo commerciale. Mais BellAuto89 est plus urgent : un essai
qui n'a jamais généré la moindre réservation, plus un rendez-vous manqué et un silence
depuis, c'est un signal de décrochage d'onboarding. Je recommande une relance courte et
concrète (« il manque juste tes prestations, on le fait ensemble en 5 min ») plutôt
qu'une relance commerciale générique. Cette relance vise un essai déjà en cours avec
contact direct d'Alexandre, donc a priori pas un sujet de démarchage à froid — mais je
ne tranche pas la nuance juridique, je la signale.

## Priorités du jour

- **BellAuto89 :** l'hypothèse d'un bouton « Prestations » bloquant est réfutée par la
  lecture du code (`ideas`) — probablement une simple interruption, pas un bug touchant
  tous les inscrits. Reste à appeler BellAuto89 (accroche courte et concrète proposée
  par `growth`) et à corriger le TODO en conséquence.
- **7 secrets GitHub** toujours à créer par Alexandre pour que la CI e2e teste
  réellement quelque chose.
- **Chantier « prospects » :** solide côté sécurité (rejet serveur du flag `is_preview`,
  vérifié indépendamment par `cyber` et `dev`) et sans risque SEO (`noindex` confirmé),
  mais `ideas` signale un point à faire trancher par `legal` : stockage de logo/couleurs
  d'un prospect avant tout consentement.
- **Code sain :** 619/619 tests verts, 0 erreur tsc/lint, 0 vulnérabilité npm côté prod.
