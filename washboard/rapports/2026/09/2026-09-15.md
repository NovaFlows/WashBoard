# Réunion d'équipe WashBoard — mardi 15 septembre 2026

## Signaux du jour

- **Commits (26 dernières heures) :** la fonctionnalité « Factures » est arrivée à maturité — vraies factures des laveurs à leurs clients (logo, mise en page sobre, laveurs en société), onglet Factures avec filtres année/mois, puis import de factures faites avant WashBoard (une par une ou en ZIP, lecture EN/FR et du symbole € avant le montant, import réservé aux factures de vente, numéro d'origine lu et modifiable). Plus un fix d'en-tête dashboard sur mobile (montants qui tiennent, « Factures » scindé en « Ventes | Achats ») et un fix sur le comptage des réservations annulées dans la feuille Clients.
- **Santé du code :** `tsc --noEmit` 0 erreur · `npm run lint` 0 erreur, 23 avertissements (inchangés, motif `set-state-in-effect` assumé) · `vitest run` **733/733 tests verts** · `npm audit --omit=dev` **0 vulnérabilité** (`npm ci` signale 3 vulnérabilités modérées, mais uniquement en dépendances de dev).
- **TODO.md — Priorité haute :** un seul point réellement ouvert — **quota Supabase dépassé**, restriction des projets prévue le **5 octobre 2026** si rien n'est fait. Cause probable : fonds/logos des pages de réservation publiques servis directement depuis Supabase Storage (sans `/_next/image`, sans cache Vercel), avec un pic de trafic lié à une vidéo TikTok virale du 2026-09-04.
- **Angle mort assumé :** aucun accès à la base Supabase de production dans cette routine — pas de signal sur l'activité réelle des clients (réservations, paiements, usage des factures). Ce n'est pas simulé ci-dessous.

## Tour de table

### seo-geo
Rien de neuf dans mon périmètre aujourd'hui (pas de balises, contenu ou sitemap touchés). Point de vigilance indirect : si le quota Supabase venait à restreindre les pages de réservation publiques le 5 octobre, ça dégraderait leur LCP et donc leur indexation — je soutiens la priorité donnée à `dev`, mais ce n'est pas mon chantier. Aucun signal trafic/GEO disponible (pas d'accès prod).

### cyber
Rien d'alarmant. J'ai vérifié spécifiquement le parsing des factures importées (ZIP + upload) : protections anti zip-bomb (taille annoncée puis vérifiée, 10 Mo/fichier, 100 Mo total, 200 fichiers), nom de fichier stocké toujours en `randomUUID` serveur (pas de path traversal via le nom fourni par le navigateur), et le DELETE d'annulation d'import échoue fermé (logge et ne supprime rien) plutôt que fail-open — exactement le réflexe demandé après les bugs RLS de la semaine. Reste à confirmer en prod que la nouvelle table `factures_importees` a bien ses policies RLS/GRANT — pas d'accès pour le vérifier moi-même aujourd'hui.

### dev
Tout est vert (tsc/lint/733 tests/audit). Sur le quota Supabase : je ne migrerais rien à l'aveugle avant d'avoir confirmé la cause exacte sur la page Usage de l'organisation (logos de factures ? assets marketing ? autre projet ?). Je regarderais aussi si les logos tout juste ajoutés pour la fonctionnalité Factures alourdissent l'egress, vu qu'ils sont probablement servis bruts depuis Storage comme le reste.

### ideas
La feature Factures pousse le produit vers de la compta — utile en démo pour Kooki Clean, mais sans retour prod sur l'usage réel, je ne me prononce pas plus loin. Le vrai sujet du jour reste le quota Supabase : un laveur qui perd l'accès à son dashboard un samedi matin casserait le pitch. Je ne propose aucune nouvelle idée aujourd'hui — pas le moment d'ajouter une fonctionnalité pendant qu'un risque d'indisponibilité reste ouvert en priorité haute.

### growth
Le pic de trafic à l'origine du quota Supabase est un signal d'acquisition positif (vidéo TikTok virale) — il ne faut pas le laisser se transformer en panne pendant qu'on l'exploite commercialement. Je relance surtout sur le témoignage Kookii Clean, mentionné en attente dans le TODO à côté de « landing livrée » : un vrai retour du bêta-testeur vaudrait plus qu'une nouvelle fonctionnalité, et le moment (page qui vient de prouver qu'elle attire du monde) s'y prête. Pas d'accès prod pour aller plus loin sur l'usage réel.

### legal
La fonctionnalité Factures est passée à du réel : vraies factures laveur→client, import de factures antérieures avec **numéro d'origine modifiable**. Point de vigilance : la numérotation doit rester chronologique et sans trou (obligation du laveur, pas de WashBoard) — à vérifier avec `dev` que l'édition du numéro à l'import ne casse pas la continuité de la séquence native. Rien d'urgent à trancher aujourd'hui ; la facturation électronique reste correctement distincte et repoussée à mi-novembre selon TODO.md, pas mélangée à cette fonctionnalité.

### designer
Le fix d'en-tête mobile du dashboard et l'onglet Factures touchent mon terrain, mais je n'ai ni capture ni retour utilisateur pour juger le rendu réel. Je note aussi que si les fonds/logos publics sont servis hors `/_next/image`, c'est potentiellement une piste d'optimisation visuelle (poids, format, lazy-loading) à auditer une fois le sujet hébergement traité par `dev`. Ce que je referais : aller vérifier ces deux écrans en vrai avec des captures avant de valider quoi que ce soit.

### prospection
Rien à signaler aujourd'hui : aucun nouveau prospect enregistré, aucune relance en attente. Le seul commit des dernières 26h touche la compta (réservations annulées), pas le fichier de prospects. J'attends les prochains numéros d'Alexandre.

### video
Rien à signaler aujourd'hui : aucun script en attente, aucune modification du projet Remotion, aucune demande d'Alexandre depuis hier. Seule note : la vidéo TikTok du 2026-09-04 continue apparemment de générer du trafic (liée au dépassement de quota egress Supabase), mais c'est un sujet infra à traiter côté dev.

## Priorités du jour

- **Quota Supabase — échéance 5 octobre 2026.** Avant de migrer quoi que ce soit, confirmer la cause exacte sur la page Usage de l'organisation Supabase (`dev`) ; regarder en particulier si les nouveaux logos de factures alourdissent l'egress en plus des fonds/logos existants. C'est le point qui prime sur toute nouvelle fonctionnalité tant qu'il reste ouvert (`ideas`, `growth`).
- **Vérifier la continuité de la numérotation des factures importées** (`legal`/`dev`) : le numéro d'origine est modifiable à l'import, à confirmer que ça ne casse pas la séquence chronologique dont le laveur est responsable.
- **Confirmer en prod les policies RLS/GRANT de la nouvelle table `factures_importees`** (`cyber`) — pas vérifiable depuis cette routine, faute d'accès à la base de production.
- **Relancer sur le témoignage Kookii Clean**, en attente depuis la landing livrée (`growth`) — moment propice vu le trafic récent sur les pages de réservation.
