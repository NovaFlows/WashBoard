# Réunion d'équipe WashBoard — jeudi 25 septembre 2026

## Signaux du jour

- **Commits (26 dernières heures)** : `ae87baa` feat(landing) — section dédiée à la facturation + correction du texte sur la réforme 2027 ; `7a47dc6` chore(rapport) — réunion d'équipe du 24/09 ; `553b6b3` merge ; `093ccfd` feat(landing) — ajout facturation et réforme 2027. Rien côté produit/dashboard, uniquement la landing.
- **Santé du code** : `npm ci` OK · `tsc --noEmit` **0 erreur** · `eslint` **0 erreur / 30 warnings** (essentiellement le motif `set-state-in-effect`, déjà assumé dans `eslint.config.mjs`, plus 2 imports/variables inutilisés mineurs) · `vitest run` **1001/1001 tests verts** (84 fichiers) · `npm audit --omit=dev` **0 vulnérabilité** en production (`npm ci` signale 3 vulnérabilités modérées, mais uniquement en dépendances de dev).
- **TODO.md — 🔴 Priorité haute**, deux points ouverts réels :
  1. **Quota Supabase dépassé** — restriction des projets annoncée à partir du **5 octobre 2026** si l'organisation reste au-dessus du quota. Cause probable : bande passante sortante (egress), les fonds/logos étant servis directement depuis Supabase Storage sans passer par le cache d'image de Next/Vercel. Plan : confirmer le poste en cause sur la page Usage Supabase, migrer vers `/_next/image`, vérifier les autres projets de l'organisation, envisager l'offre Pro si besoin avant le 5/10.
  2. **Audit post-lancement du 21/09** (fait par Ryan) — 15 des 20 points vérifiés sont déjà en place, **5 restent** : vitesse de la page de réservation (LCP 3,1 s, dont 2,3 s de TTFB, mesure unique à refaire), contraste des couleurs (WCAG AA, plusieurs éléments sous le seuil dont l'onglet actif de la page de réservation), image de partage manquante sur l'accueil (pas d'`og:image`/`twitter:image`), CGU absentes, et acceptation des CGV non tracée à l'inscription. **Question en attente pour Alexandre** : est-ce Ryan qui prend ces 5 points, ou lui.
- **Signal manquant, assumé plutôt que simulé** : aucun accès à la base Supabase de production dans cette routine — rien à dire sur l'activité réelle des clients (réservations, paiements, trafic Vercel Analytics).

## Tour de table

### seo-geo
J'ai confirmé dans `layout.tsx` que les blocs `openGraph` et `twitter` de l'accueil n'ont aucun champ `images` — le `twitter:card: "summary_large_image"` est donc incohérent avec ce qu'il fournit. Comme c'est le lien le plus partagé, ça dégrade les aperçus sur réseaux sociaux et potentiellement les extraits IA. Je laisse `designer` porter le correctif visuel, mais je recommande de dériver le texte alt/description depuis les constantes déjà utilisées pour le meta `description`, pour rester cohérent. Rien à signaler côté sitemap/JSON-LD/blog aujourd'hui ; pas d'avis sur le quota Supabase, hors de mon terrain.

### cyber
Rien de neuf côté sécurité dans les commits du jour (landing, pas de nouvelle surface). `npm audit --omit=dev` à 0 confirme l'état de prod, les 3 modérées ne concernent que des devDependencies. Je relirai le point 5 de l'audit Ryan (case CGV + date à l'inscription) dès que legal/dev l'auront implémenté, car ça touche un flux d'autorisation. Je referais : rester vigilant sur le quota Supabase — si la restriction du 5 octobre force un repli technique en urgence, je veux voir le code avant qu'il parte en prod, pour éviter un cinquième cas du motif « échec silencieux → comportement permissif » déjà rencontré sur ce projet.

### dev
Rien de nouveau côté code (0 erreur tsc, lint propre, 1001 tests verts) ; les deux points chauds sont hors code, sur mon dossier. Le quota Supabase pointe vers `getBgStyle`, qui sert fonds/logos hors `/_next/image` donc sans cache Vercel — à confirmer côté Usage Supabase avant de migrer, échéance 5/10. Le LCP 3,1 s (2,3 s de TTFB) de la page de réservation est à remesurer proprement (hors démarrage à froid) avant toute conclusion. Sur la case CGU/CGV, je passe par `legal` pour la spec avant d'écrire une ligne, avec relecture `cyber` ensuite.

### ideas
Rien côté produit qui mérite un jugement aujourd'hui — les commits sont éditoriaux/commerciaux, pas une feature à trancher. Les deux points 🔴 sont opérationnels (quota, audit), pas des idées à filtrer avec ma grille : le quota est un sujet dev/cyber. Pas de donnée d'usage réel aujourd'hui, donc aucun signal pour proposer quoi que ce soit — je ne fabrique pas d'idée sans ça. Je referais la même chose demain si l'accès Supabase de prod ne revient pas.

### growth
Toujours pas de visibilité sur l'usage réel (réservations, rétention) — je ne peux pas confirmer si un client qui avait arrêté de réserver a repris. Le quota Supabase (restriction dès le 5/10) m'inquiète côté commercial : la page de réservation publique est mon outil de démo pour la bêta Kooki Clean, une coupure la rendrait inutilisable en plein argumentaire. Rien à changer sur l'argumentaire aujourd'hui, pas de nouveau signal de `prospection`. Je demanderai à Alexandre un point direct sur l'usage Kooki Clean plutôt que d'attendre un accès Supabase.

### legal
Les points 4 et 5 de l'audit Ryan me concernent directement : CGU à créer et case d'acceptation CGU+CGV+confidentialité à l'inscription, avec date horodatée pour être opposable en cas de litige (impayé, résiliation contestée). Je peux fournir le texte dès qu'Alexandre tranche qui pilote (Ryan ou moi) — je ne rédige rien aujourd'hui, c'est un point de statut. Point d'attention : la case doit être non pré-cochée et le texte des CGV/confidentialité qu'elle référence doit déjà être à jour. Je referais : demander l'arbitrage d'Alexandre avant de commencer, pour éviter deux versions concurrentes des CGU en parallèle.

### designer
Rien de neuf côté commits (facturation, hors de mon ressort visuel). Je confirme sur pièces les deux points de l'audit Ryan qui me concernent : `text-slate-400` illisible sur fond blanc (pied de page, dates d'articles, mentions légales), et l'onglet actif de la page de réservation quasiment invisible en bleu clair sur fond bleuté — pas cosmétique vu que c'est la page qu'Alexandre montre en démo. Je prépare les corrections de contraste (couleurs plus foncées, sans toucher police/palette/accent laveur) plus l'image de partage manquante sur l'accueil, mais rien n'est poussé aujourd'hui ; je fournirai un avant/après dès que je code le correctif.

### prospection
Rien à signaler aujourd'hui, aucun numéro reçu d'Alexandre depuis le dernier point — le fichier prospects reste en l'état, aucune analyse en attente. Prêt dès qu'il envoie des contacts.

### video
Rien à signaler aujourd'hui — aucune demande d'Alexandre en cours, ni voix-off ni rendu prévu pendant cette réunion.

### sentry
Rien à signaler aujourd'hui, aucune alerte Sentry ni `errorId`/`digest` remonté. Code sain (tsc 0, lint 0 erreur, 1001/1001 tests verts), pas d'accès prod dans cette routine donc rien à corréler côté Supabase. Veille sur les commits landing au cas où ils toucheraient des flux surveillés, mais aucune investigation en cours.

### analytics
Rien à signaler côté chiffres : pas d'accès Vercel Web Analytics configuré dans cette routine, et pas d'accès à la base Supabase de prod, donc aucun funnel CRM réel disponible aujourd'hui. Rien à corréler avec les commits du jour faute de données. Aucun chiffre inventé pour combler ce manque — à réévaluer dès qu'un accès (jeton Vercel ou export manuel d'Alexandre) sera disponible.

## Priorités du jour

- **Trancher qui pilote les 5 points restants de l'audit post-lancement** (vitesse, contraste, image de partage, CGU, acceptation CGV) — Ryan ou Alexandre — avant le 5 octobre si possible, vu le lien avec le quota Supabase.
- **Quota Supabase (échéance 5 octobre)** : confirmer le poste en cause (egress probable, fonds/logos hors cache Next), migrer si confirmé — sujet commun dev/cyber, risque direct sur la démo commerciale (growth) si la page de réservation venait à être restreinte.
- **Contraste + image de partage de l'accueil** : designer a confirmé les deux constats sur pièces (dont l'onglet de la page de démo) et prépare les correctifs, à valider avant mise en prod.
- **CGU / acceptation CGV à l'inscription** : legal prêt à fournir les textes dès l'arbitrage ci-dessus, dev/cyber en aval — aucun texte ni code encore engagé.
