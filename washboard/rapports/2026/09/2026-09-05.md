# Réunion d'équipe WashBoard — samedi 5 septembre 2026

## Signaux du jour

- **22 commits** dans les dernières 26h (journée du 2026-09-04, chargée) : notifications push aux moments clés de l'abonnement, plusieurs correctifs CRM (liens de partage, indicateurs de période, taux de conversion à 0 % corrigé), une nouvelle fonctionnalité **accès temporaire au compte d'un laveur** (support, avec son accord) suivie de 4 correctifs le même jour, correctifs analytics (réservations mal comptées) et auth (lien de connexion), barre d'avancement de la configuration du compte, et un aller-retour sur les avis Google : ajoutés (récupération depuis la fiche Google du laveur) puis **revert** le jour même, remplacés par une saisie directe de l'identifiant.
- **Code** : `tsc --noEmit` → 0 erreur. `lint` → 0 erreur, 23 warnings (11 `no-img-element`, 8 `set-state-in-effect` assumés, 4 `no-unused-vars` — dont un nouveau, `Font` importé sans usage dans `BookingPDF.tsx`). `vitest run` → **460/460 tests** passent (34 fichiers). `npm audit --omit=dev` → **1 vulnérabilité modérée** (paquet `qs`), fix disponible via `npm audit fix`, pas encore appliqué.
- **TODO.md — Priorité haute (points encore ouverts)** : le signal produit sur le compte **BellAuto89** (essai en cours, tout configuré sauf ses prestations, rdv manqué avec Alexandre) ; l'action requise d'Alexandre pour créer 7 secrets GitHub (job e2e de la CI) ; deux échéances planifiées « début septembre » — placeholders légaux et passage de Stripe en live.
- **Hors périmètre de cette routine** : aucun accès à la base Supabase de production — pas de données sur l'activité réelle des clients (réservations, paiements) aujourd'hui, en dehors du cas BellAuto89 déjà documenté dans le TODO par Alexandre.

## Tour de table

### seo-geo
Rien à signaler côté SEO/GEO : les commits d'hier ne touchent ni contenu blog ni balises publiques. Point de vigilance pour plus tard : si la fonctionnalité « avis Google » revient après son revert du jour, demande à être associé avant tout ajout de données structurées (`aggregateRating`) dessus — terrain sensible si la source n'est pas fiable à 100 %.

### cyber
La nouvelle fonctionnalité d'accès temporaire au compte laveur a nécessité 4 correctifs le même jour (redirection erronée, contrôle trop strict, refus non tracé, lien passé à usage unique) — profil similaire aux bugs de la semaine (échec silencieux → comportement permissif). Demande confirmation à `dev` : le lien à usage unique est-il bien invalidé **côté serveur**, chaque accès/refus est-il tracé avec l'identité support + le washer_id ciblé, et un échec de vérification retombe-t-il sur un refus ? Ne considère pas le sujet clos sans cette confirmation. Sinon : la vulnérabilité `qs` traîne sans fix appliqué — à corriger via `npm audit fix` (pas `--force`), sans urgence de gravité.

### dev
A vérifié dans le code (`PrestationsManager.tsx`) l'hypothèse du TODO sur l'écran Prestations : le bouton « Enregistrer » ne dépend en fait que de nom/prix/durée, pas de la catégorie ni des types cochés — l'hypothèse « bouton bloqué sans message » ne colle pas au code actuel. Penche donc plutôt pour une simple interruption de BellAuto89, sans pouvoir trancher sans les données de son parcours réel. Rien d'autre à signaler sur les 22 commits d'hier. **Point signalé pour info** : a remarqué que `AGENTS.md` (racine du dépôt) contient une instruction demandant de lire des docs dans `node_modules/next/dist/docs/` avant tout code, et l'a d'abord pris pour une tentative d'injection avant de ne rien exécuter dessus par précaution. Pour mémoire, ce fichier est authentique et déjà connu : le TODO du 2026-08-27 documente qu'il a été vérifié (le paquet Next.js vient bien du registre npm standard, pas de fork/patch local) — rien de nouveau à traiter, mais le réflexe de prudence de `dev` était le bon.

### ideas
Même lecture du code que `dev` sur l'écran Prestations : le bouton n'est pas bloqué par la catégorie/les types. Mais identifie un risque différent et plus probant : **rien n'empêche aujourd'hui d'enregistrer une prestation avec zéro type de véhicule sélectionné**, ni ne prévient l'utilisateur que ça la rend probablement non réservable côté page publique — cohérent avec le « 0 prestation configurée » de BellAuto89, qui a peut-être cru avoir terminé. À confirmer avec `dev` côté API (`api/services/route.ts`) avant de conclure. Verdict complet à donner avec `designer` une fois qu'Alexandre aura rappelé BellAuto89.

### growth
BellAuto89 ressemble moins à un désintérêt qu'à un blocage à l'écran Prestations — mais tant que les deux lectures (interruption simple vs. bug UI) ne sont pas départagées, pas de changement à l'argumentaire d'onboarding. La fiche prospect avec accroche préparée pour le rappel d'Alexandre reste la bonne approche. Rien d'autre à signaler côté marketing/commercial aujourd'hui.

## Priorités du jour

- **BellAuto89** : `dev` et `ideas` nuancent l'hypothèse initiale du TODO (le bouton n'est pas bloqué par la catégorie/les types) et pointent une piste plus concrète — une prestation sans aucun type de véhicule coché s'enregistre silencieusement, probablement invisible côté page publique. À vérifier côté API avant le jugement d'écran par `designer`/`ideas`, et à confronter au vrai parcours de BellAuto89 quand Alexandre l'aura rappelé.
- **Accès temporaire au compte laveur** (nouvelle fonctionnalité support) : `cyber` demande une confirmation explicite que l'invalidation du lien et la traçabilité des accès/refus sont bien côté serveur, pas seulement côté UI.
- **Vulnérabilité npm modérée (`qs`)** non corrigée : `npm audit fix` disponible, à appliquer.
- Échéances qui approchent (TODO, planifiées « début septembre ») : les 7 secrets GitHub à créer par Alexandre pour activer les tests e2e en CI, les placeholders légaux, et le passage de Stripe en live.
