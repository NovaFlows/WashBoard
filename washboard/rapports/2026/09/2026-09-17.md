# Réunion d'équipe WashBoard — jeudi 17 septembre 2026

## Signaux du jour

- **Commits (26 dernières heures)** : `fb54c7c` traçage des 5 dernières lectures silencieuses
  de `lib/` (google-calendar.ts, materializeRecurring.ts, travelFee.ts) — clôt l'audit
  « lectures qui échouent en silence » ; `a1a6b4e` garde-fou « client historique » écarté par
  Alexandre (non traité, décision assumée) ; `3c98282` PDF de facture public conservé mais
  header `X-Robots-Tag: noindex, nofollow, noarchive` ajouté ; `4269a80` prix dans la
  notification vérifié en prod ; `6649379` prix affiché en réservation + rappel du soir tracé ;
  `532d471` lecture des aperçus page par page ; `ceebb56`/`3ee3666` badge calendrier et
  confirmation de clôture ; `d5a463d` facture manuelle envoyée aussi au client pro.
- **Santé du code** : `npm ci` OK · `tsc --noEmit` 0 erreur · `npm run lint` 0 erreur (22
  warnings assumés, motif `set-state-in-effect`) · `vitest run` **783/783 tests verts** (62
  fichiers) · `npm audit --omit=dev` **0 vulnérabilité**.
- **TODO — Priorité haute** : un seul sujet ouvert et chiffré, chargé d'urgence — **quota
  Supabase dépassé (bande passante egress)**, deadline **5 octobre 2026** (restriction du
  projet sinon, qui couperait les pages de réservation de tous les laveurs). Cause probable :
  fonds/logos servis directement depuis Supabase storage sans passer par l'optimisation
  d'images Next (`/_next/image`), aggravé par une vidéo TikTok virale du 4 septembre. Reste à
  faire : confirmer le poste de consommation sur la page Usage Supabase, migrer vers
  `/_next/image`, vérifier qu'aucun autre projet de l'organisation ne consomme, envisager
  l'offre Pro Supabase avant le 5/10 si besoin.
- **Signal manquant** : aucun accès à la base Supabase de production — rien à dire sur
  l'activité réelle des clients (réservations, paiements, effet concret du pic TikTok).

## Tour de table

### seo-geo
Rien de neuf côté SEO/GEO — les correctifs du jour ne touchent ni balises ni sitemap. Seul
point de vigilance indirect : si le quota Supabase finit par restreindre le projet début
octobre, des pages indexées deviendraient inaccessibles — un souci de disponibilité, pas de
contenu, donc du ressort de `dev`. Rien à changer sur son terrain aujourd'hui.

### cyber
Approuve les trois correctifs de traçage (gcal, materializeRecurring, travelFee) : ils
suivent le motif identifié cette semaine. Le noindex sur le PDF de facture est une bonne
défense en profondeur, mais reste un lien public devinable-si-fuité (SIRET, adresses,
montant) — à garder en tête, pas bloquant aujourd'hui. Point qui le gêne réellement : le
garde-fou « client historique » écarté sans traitement — `grandfathered=true` seul comme
condition d'exemption de paiement est une logique fragile ; il redemande explicitement un
arbitrage d'Alexandre, sans trancher lui-même. Le quota Supabase egress est un sujet
dev/perf, pas sécurité.

### dev
L'audit « lectures silencieuses » est clos — bon signal, plus de fallback permissif muet
dans `lib/`. Sujet chaud : l'egress Supabase, cause probable identifiée
(`getBgStyle`/images non optimisées) mais pas encore confirmée sur la page Usage — deadline
5/10 proche. Vérifierait d'abord les chiffres réels avant de se lancer dans la migration
`/_next/image`, pour ne pas corriger la mauvaise cause pendant que la deadline tourne. Garde
PayPal en attente de décision d'Alexandre. Rien à signaler côté santé du code ni sur les
composants monolithiques (statu quo assumé).

### ideas
L'egress Supabase est le vrai sujet — c'est du produit qui casse, pas une fonctionnalité en
attente : priorité absolue devant tout le reste. Sur PayPal, ne tranche pas à sa place :
choix produit/risque qui revient à Alexandre. Une piste, pas un feu vert : le pic TikTok est
un signal d'intérêt organique réel, mais corriger la cause technique (Storage non optimisé)
doit venir d'abord, sinon chaque pic viral futur redéclenche le même quota — la question du
canal ne se pose qu'ensuite, si le signal se répète.

### growth
Le pic TikTok du 4/09 est le seul vrai signal de traction organique disponible, mais
aveugle côté conversion (pas d'accès Supabase prod) — l'egress est traité côté dev, pas son
sujet. Le vrai risque business qu'il pointe : le trou PayPal (aucune alerte à Alexandre
quand un paiement attend hors heures ouvrées) peut tuer un momentum d'inscription généré
justement par ce genre de pic viral. Rien de neuf côté argumentaire aujourd'hui — pas
d'improvisation sans données de conversion réelles.

### legal
Le header noindex règle l'indexation par les moteurs mais pas l'accès par lien : une URL
non authentifiée avec des données perso/pro (SIRET, adresse, montant) reste divulguée à
quiconque l'obtient — « noindex » n'est pas un contrôle d'accès. Choix produit assumé par
Alexandre (contrainte de remise par email), pas à retoquer, mais exposition résiduelle
réelle si le lien fuite. Suggère de documenter ce compromis pour pouvoir le justifier, et
que `cyber` regarde un jour un token à durée de vie/rotation sur ces liens, sans bloquer la
décision actuelle. Rien de neuf côté garde-fou « client historique » — sujet
produit/facturation, pas juridique à première vue.

### designer
Rien de neuf côté UI/UX aujourd'hui : les commits du jour sont uniquement backend, et le
TODO prioritaire est infra pur (pas un sujet de design, même si compresser des assets
visuels pourrait faire partie de la solution si `dev` le demande). Le focus trap sur
`ConfirmerCloture` reste ouvert et mineur, toujours pas tranché priorité — ne le remonte pas
de lui-même sans usage tactile documenté.

### prospection
Fichier de prospects vérifié : vide, aucun statut « à relancer » ni « RDV pris ». Rien à
signaler aujourd'hui.

### video
Rien à signaler aujourd'hui.

## Priorités du jour

- **Quota Supabase (egress) — deadline 5 octobre.** Le sujet unanimement identifié comme
  prioritaire (`dev`, `ideas`, `seo-geo`) : confirmer la cause sur la page Usage Supabase
  puis migrer fonds/logos vers `/_next/image` avant la restriction.
- **Arbitrage à demander à Alexandre : garde-fou « client historique ».** `cyber` redemande
  explicitement un tranchage sur `grandfathered=true` comme condition d'exemption de
  paiement — jugé fragile en l'état, écarté sans traitement le 17/09.
- **PayPal : un laveur qui paie hors heures ouvrées reste bloqué sans alerte.** Remonté par
  `dev` et `growth` : risque de perte de momentum d'inscription, notamment après un pic
  viral comme celui du 4/09 — décision (statu quo / bouton admin / webhook) toujours en
  attente d'Alexandre.
- **PDF de facture public : noindex ajouté, mais pas de contrôle d'accès.** `legal` propose
  de documenter le compromis et de demander à `cyber` d'évaluer un jour un lien à durée de
  vie — sans bloquer la décision produit actuelle.
