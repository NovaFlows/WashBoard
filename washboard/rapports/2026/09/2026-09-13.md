# Réunion d'équipe WashBoard — dimanche 13 septembre 2026

## Signaux du jour

**Commits (dernières 26h) :** 8 commits produit + le rapport de la veille — nouveau logo (marque),
fix session (l'app installée ne se déconnectait plus sans raison), 4 commits CRM (trafic dans le
temps, nuage des sources, CA cumulé, délai de réservation, fix pagination 1000 lignes, fix message
dupliqué), refactor du tableau de bord réservations, et un ajustement des métadonnées SEO sur
l'élargissement du positionnement au-delà du lavage auto.

**Santé du code :** `npm ci` OK · `tsc --noEmit` 0 erreur · `lint` 23 warnings, 0 erreur (motif
`set-state-in-effect` assumé, connu) · `vitest run` **658/658 tests verts** (49 fichiers) ·
`npm audit --omit=dev` **0 vulnérabilité**. Rien de cassé, rien de nouveau côté fiabilité.

**TODO — Priorité haute (point ouvert depuis plusieurs jours) :** BellAuto89 (essai en cours,
inscrit le 2026-08-20) a configuré logo, adresse et 28 créneaux mais 0 prestation → 0 réservation
possible, et n'est pas venu à son rendez-vous avec Alexandre. Le TODO demandait de trancher entre
simple interruption et écran Prestations bloquant. *(Voir tour de table `ideas` ci-dessous — élément
factuel nouveau depuis la dernière fois que ce point a été rapporté.)*

⚠️ Aucun accès à la base Supabase de production : pas de signal sur l'activité réelle des clients
(réservations, paiements, trafic réel).

## Tour de table

### seo-geo
Le commit d'aujourd'hui étend title/meta/OG/JSON-LD à un positionnement multi-métiers (ménage,
piscine, detailing) alors que le blog reste volontairement 100 % lavage auto — les balises promettent
un périmètre que le contenu ne couvre pas encore, ce qui dilue le signal SEO/GEO. Aurait gardé
« lavage auto mobile » en tête de gondole et placé l'élargissement plus loin dans la description,
comme le fait déjà le footer de la landing. Pas de retouche de balises aujourd'hui : point à trancher
avec `growth` avant de recavaler entre deux formulations à chaque commit.

### cyber
Les deux commits qui le concernent (fix session, fix CRM) appliquent la discipline attendue : la
session ne se déconnecte plus sur un faux positif, et l'échec de lecture des événements CRM est
désormais loggué au lieu d'un échec silencieux — exactement le motif qu'il traque depuis les bugs de
congés/honeypot. Rien à corriger. Point mineur non bloquant relevé : `dashboard/page.tsx` fait encore
un `?? []` sans logger l'erreur (compteurs d'affichage seulement, pas une décision d'autorisation) —
à traiter la prochaine fois qu'on touche ce fichier, pas urgent.

### dev
Le motif des bugs passés (lecture qui échoue en silence, repli permissif) est le même que celui de la
pagination Supabase à 1000 lignes : les 8 lectures `bookings` non paginées restantes (calendrier,
compta, page de réservation publique, créneaux malins, dashboard, debug) restent la dette la plus
risquée pour la confiance client. Propose de traiter en priorité la page de réservation publique et le
calendrier, avec le même helper `toutesLesLignes.ts` que le CRM. N'a pas touché à l'écran Prestations
de BellAuto89 en attendant le verdict de `designer`/`ideas` — prêt à coder un message d'erreur explicite
dès que la décision tombe.

### ideas
A vérifié le code de `PrestationsManager.tsx` : la version en prod (commit du 06/09, **avant**
l'inscription de BellAuto89) corrige déjà le scénario décrit — choisir une catégorie coche
automatiquement ses types, et le formulaire vierge présélectionne la première catégorie. Le
commentaire du code décrit mot pour mot le problème du TODO, signe qu'il a déjà été corrigé une
première fois. L'hypothèse « écran structurellement bloquant » ne tient donc plus sur le code actuel.
Reclasserait ce cas en simple interruption plutôt qu'en bug généralisé — reste une friction mineure
non préremplie (prix/durée) qui ne dit pas pourquoi le bouton est désactivé, sans commune mesure avec
le blocage total décrit. Demande à `dev` de confirmer que la prod correspond bien à ce commit avant de
clore le point.

### growth
BellAuto89 reste le scénario qui inquiète le plus pour la conversion essai→abonnement : blocage sur
Prestations, jamais vu le produit fonctionner, RDV manqué, trois semaines de silence sans savoir si
c'est de la friction produit ou un laveur débordé. Stripe n'étant pas encore en live, l'onboarding
paiement reste manuel de toute façon — ce n'est pas la cause ici. Rien à changer sur le pitch ou le
discours commercial : ce n'est pas un problème de conviction mais de « peut-il essayer le produit ».
Recommande d'attendre l'issue de l'appel d'Alexandre avant toute action, et de faire remonter via
`ideas` plutôt que d'improviser une relance commerciale.

## Priorités du jour

- **BellAuto89 : élément nouveau à vérifier avant de rappeler.** `ideas` a trouvé que le correctif de
  l'écran Prestations existait déjà en prod avant l'inscription de BellAuto89 — l'hypothèse « écran
  bloquant » ne colle plus au code actuel. `dev` doit confirmer que la prod correspond bien à ce
  commit ; si oui, ce point du TODO peut être reclassé en simple interruption plutôt que remonté
  chaque jour, et l'appel de rappel peut se faire sans attendre un jugement `designer`.
- **Positionnement SEO élargi vs contenu blog resté lavage auto** : `seo-geo` signale un décalage entre
  les balises (multi-métiers) et le contenu structurant (100 % lavage auto) — à trancher avec `growth`
  sur le séquencement plutôt que de retoucher les balises à chaque commit.
- **8 lectures `bookings` encore non paginées** (calendrier, compta, réservation publique, créneaux
  malins, dashboard, debug) : aucune ne tronque encore, mais `dev` recommande de traiter la page de
  réservation publique et le calendrier en priorité avec le helper déjà existant.
- Rien de cassé côté code (tsc/lint/tests/audit tous verts) — aucune action requise sur ce plan.
