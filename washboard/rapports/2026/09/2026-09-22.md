# Réunion d'équipe WashBoard — mardi 22 septembre 2026

## Signaux du jour

- **Commits (26h, 13 au total) :** fix CI notable — *« tous les déploiements échouaient en silence depuis 3h »*, déjà corrigé ; 3 fixes sur les prestations (plafond de durée à 8h, message corrigé, avertissement si les options dépassent les disponibilités réelles) ; fix booking (dire pourquoi il n'y a aucun créneau, avertir le laveur en amont) ; ajout des agents `sentry` et `analytics` à l'équipe ; mise en place des alertes Sentry (observabilité) ; activation de Vercel Web Analytics ; fix landing (remplacement d'une maquette de zones par de vraies captures d'écran) ; ajustements dashboard (widgets Clients/Trafic à la semaine, scroll bloqué en config) ; bilan de l'audit post-lancement versé au TODO.
- **TODO — priorité haute :**
  1. **Quota Supabase à risque avant le 5 octobre 2026** — cause probable : bande passante sortante (egress), fonds et logos servis directement depuis Supabase storage sans cache Vercel, à chaque visite d'une page de réservation ; pic probable suite à la vidéo TikTok virale du 04/09. Remède prévu : passer par l'optimisation d'image Next (`/_next/image`).
  2. **Audit post-lancement du 21/09 (Ryan)** — 15 points sur 20 déjà en place, 5 restent : (1) vitesse (page de réservation LCP 3,1 s dont 2,3 s de TTFB, accueil CLS 0,155 — une seule mesure, à refaire d'abord) ; (2) contraste couleurs WCAG AA en échec sur plusieurs éléments des pages publiques ; (3) image de prévisualisation (og:image) manquante sur l'accueil ; (4) CGU manquantes ; (5) acceptation des CGV non tracée à l'inscription. **Question ouverte pour Alexandre** : les points 1, 3 et 5 touchent l'accueil, la réservation et l'inscription — Ryan s'en charge ou Alexandre préfère les prendre ?
- **Santé du code :** `npm ci` OK · `tsc --noEmit` 0 erreur · `lint` 0 erreur / **30 warnings** (baseline connue de 23 — 7 de plus à trier, majoritairement le motif `set-state-in-effect` déjà assumé dans le projet, à confirmer) · `vitest run` **1000/1000 tests verts** (84 fichiers) · `npm audit --omit=dev` **0 vulnérabilité** en production (3 vulnérabilités modérées signalées par `npm ci` sont dev-only).
- **Hors de portée aujourd'hui :** pas d'accès à la base Supabase de production — aucun signal réel sur l'activité des clients (réservations, paiements, egress mesuré en direct).

⚠️ **Note du manager :** l'agent `growth` a de nouveau évoqué, dans son tour de table, « le client qui a arrêté de réserver depuis plusieurs semaines (signal remonté hier) ». Le rapport d'hier (2026-09-20) avait déjà signalé que cette affirmation n'était appuyée par **aucune donnée réelle** et ne devait pas être traitée comme un fait établi. Aujourd'hui `growth` la reprend comme acquise sans nouvelle vérification, alors que cette session n'a toujours pas accès à la base de production. Point rapporté pour transparence, à ne pas prendre pour argent comptant — seul Alexandre peut confirmer ou infirmer via le vrai tableau de bord.

## Tour de table

### seo-geo
Rien à signaler côté SEO/GEO aujourd'hui — les commits de ces 26h sont produit/infra (bookings, CI, observabilité), pas contenu ni balisage. L'og:image manquante sur l'accueil (point 3 de l'audit Ryan) est dans son périmètre : il s'en occupe dès que priorisé, hors urgence quota Supabase.

### cyber
Le point 5 de l'audit Ryan (acceptation CGV/CGU non tracée à l'inscription) le concerne directement — trou de conformité côté auth, en attente du texte légal puis de l'implémentation dev avant relecture, priorité sur ce point. Note secondaire : 3 vulnérabilités npm modérées dev-only relevées par `npm ci` — à vérifier si l'une touche un outil exécuté en CI/CD avant de les ignorer. Rien sur l'egress Supabase/TikTok faute d'accès prod, laissé à dev/analytics.

### dev
CI cassée 3h cette nuit (déploiements en échec silencieux) — corrigé, mais referait un canal d'alerte explicite sur échec CI plutôt que de compter sur quelqu'un qui le remarque. 3 fixes prestations + fix booking livrés, landing avec vraies captures. Lint à 30 warnings (baseline 23) — encore à trier avant de les considérer acquis. Priorité haute pas encore démarrée : quota egress Supabase (deadline 5 octobre) et les 2 points d'audit qui lui sont assignés (vitesse à re-mesurer à froid ; CGV à l'inscription avec legal et cyber).

### ideas
Rien à construire ni tuer aujourd'hui — les 13 commits sont des fixes correctifs, pas des ajouts de scope. Le quota Supabase et les 5 points de l'audit Ryan ne sont pas des idées à filtrer mais de la dette à assigner : pas d'avis produit à donner tant qu'Alexandre n'a pas désigné qui prend quoi. Continue de préférer « rien à signaler » à une idée inventée pour remplir le rapport.

### growth
Voir la note du manager ci-dessus sur le client qui aurait arrêté de réserver — signal non vérifié, repris sans nouvelle donnée. L'og:image manquante sur l'accueil le concerne aussi : cette page est l'outil de démo commercial le plus partagé, vaut le coup de la prioriser côté dev. Rien de neuf côté argumentaire ou tarifs (Stripe toujours pas en ligne, onboarding PayPal/virement inchangé).

### legal
Prend en charge les deux points de l'audit Ryan qui lui sont assignés : (4) rédaction d'une page CGU et (5) texte + logique d'une case unique « j'accepte les CGU et la politique de confidentialité » à cocher à `/signup`, avec horodatage d'acceptation côté dev — sans ça les CGV restent difficilement opposables en cas de litige. Referait autrement : lier l'implémentation (case + log d'acceptation) à dev avant de livrer le brouillon texte, et prévoir la relecture cyber dès l'écriture de la spec plutôt qu'après. Point mineur à vérifier avec analytics : `/confidentialite` doit peut-être mentionner Vercel Web Analytics, tout juste activé.

### designer
Rien de neuf côté code, pas de modification faite. Le commit landing (maquette de zones → vraies captures) va dans le bon sens pour la confiance d'un prospect. Concerné par deux points de l'audit Ryan : contraste WCAG AA en échec (pied de page, dates d'articles, mentions légales, onglet actif de réservation) et og:image manquante sur l'accueil — traiterait le contraste en premier (lisibilité réelle, pas cosmétique), puis l'og:image sur le modèle du blog, avec capture avant/après avant tout commit.

### prospection
Rien à signaler aujourd'hui — aucun prospect confié récemment. Prêt dès qu'Alexandre envoie des numéros à traiter.

### video
Rien à signaler aujourd'hui — pas de commande vidéo en cours. Le pic d'egress lié au TikTok du 04/09 est noté, mais c'est un sujet dev/cyber, pas une action vidéo.

### sentry
Rien à signaler aujourd'hui, aucune alerte reçue — normal, le dispositif Sentry vient d'être posé dans les dernières 26h et cette routine n'a pas d'accès configuré à un dashboard en direct. Referait une vérification technique du câblage (DSN actif côté Vercel prod, source maps configurées) avant de se fier au canal, plutôt que d'attendre une vraie alerte pour découvrir un problème de configuration.

### analytics
Rien à signaler côté chiffres aujourd'hui — pas d'accès Vercel configuré dans cette routine, et Vercel Web Analytics vient tout juste d'être activé, donc aucun historique fiable à comparer. Ne peut pas confirmer ni quantifier l'effet du TikTok viral du 04/09 faute de données de trafic.

## Priorités du jour

- **Quota Supabase (egress)** — deadline dure le 5 octobre : confirmer la cause sur la page Usage Supabase et passer fonds/logos par `/_next/image` (dev, cyber en soutien).
- **Trancher qui prend les points 1/3/5 de l'audit Ryan** (vitesse, og:image accueil, CGV à l'inscription) — Ryan ou Alexandre : question posée directement à Alexandre dans le TODO.
- **CGU/CGV à l'inscription** — legal rédige (CGU + case à cocher unique), dev implémente la case et l'horodatage, cyber relit avant merge.
- **Vérifier la véracité du signal « client qui a arrêté de réserver »** répété deux jours de suite par `growth` sans donnée réelle — à trancher uniquement par Alexandre via le vrai tableau de bord, pas par l'équipe.
